# Review: phase-1-gate — 2026-06-10
Base: 77c4c65 (Phase 0 exit)  Head: c1e206f (phase-1 head; diff scope `git diff 77c4c65...c1e206f`)  Verdict: BLOCK
Protected crate touched: yes (crates/fortuna-invariants/ — 3 test files; automatic BLOCK pending operator review)

Grading mode: PHASE-GATE (rubric = BUILD_PLAN.md Phase 1 EXIT line, lines 121-122, plus the
PROMPT.md acceptance item it references, PROMPT.md lines 106-107: "...in Paper against
recorded/streamed data with realistic fills"). The "EXIT MET TO THE BUILDABLE EXTENT"
annotation under the EXIT line was treated as implementer claim, not evidence.

Execution note (required): all executable checks (fmt, clippy, tests, DST) were run at the
CURRENT HEAD 1a113ee, which additionally contains T2.1 (a Phase 2 task). Nothing was checked
out in the operator's working tree. Because the operator's working tree is dirty with
uncommitted T2.2-in-progress files (crates/fortuna-cognition/src/signals.rs et al.) that fail
`cargo fmt --check` and break `fortuna-ledger` compilation under clippy, the battery was
executed in a detached scratch worktree at the SAME commit (`git worktree add --detach
/tmp/fortuna-gate-head 1a113ee`). Dirty-tree results are reported under Finding F4 for
operator awareness; they are not Phase 1 evidence.

## Criteria (fixed before reading the diff)

- C1 Both mechanical strategies (mech_structural, mech_extremes) run in Paper against
  recorded data streams, with realistic fills (BUILD_PLAN.md:121; PROMPT.md:106-107):
  **UNVERIFIABLE — fails the gate.** No recorded data streams exist: `fixtures/kalshi/`
  contains only README.md (verified by `ls -R fixtures/`). No runner composition feeds
  PaperVenue (grep `PaperVenue` in crates/fortuna-runner/src/ — zero hits). The dependency is
  operator-blocked and documented in GAPS.md lines 66-76: "**Paper engine awaits recorded
  Kalshi data streams (T1.2, 2026-06-10; reaffirmed at Phase 1 exit).** ... OPERATOR-BLOCKED
  beyond it: the fixture capture must include websocket `orderbook_snapshot`/`orderbook_delta`
  and public `trade` messages. First task once fixtures exist: the venue-generic runner
  composition ... replaying recorded streams into PaperVenue under both strategies."
  Buildable portion independently verified green: sim_loop (7 passed), mech_extremes
  (15 passed), fortuna-paper (11 passed, incl. `paper_live_parity_through_the_order_manager`,
  paper.rs:362). Per phase-gate rules an UNVERIFIABLE item fails the gate; no partial credit.

- C2 Settlement and discrepancy paths exercised in DST (BUILD_PLAN.md:121-122): **FAIL.**
  Settlement half holds: crates/fortuna-core/tests/dst.rs:435 ("settle | void | reverse (1%;
  spec 5.13 lifecycle paths)") drives `settle_market`/`void_market`/`reverse_settlement`
  against the sim venue with I-money checked through refunds/claw-repay; executed run:
  `scripts/run-dst.sh 3000` → "[dst] OK: 0 corpus + 3000 random seeds, zero invariant
  violations" (master seed 1781106172459, DST-EXIT:0). Discrepancy half does NOT hold:
  `grep -i discrepanc crates/fortuna-core/tests/dst.rs` → zero hits; scripts/run-dst.sh
  executes only `cargo test -p fortuna-core --test dst`, so the DST harness never reaches the
  discrepancy machinery (3-tick position-mismatch → discrepancy record → GLOBAL halt). That
  path is exercised only in deterministic composed-loop integration tests
  (crates/fortuna-runner/tests/settlement_loop.rs:406
  `persistent_position_mismatch_writes_discrepancy_and_halts`, 8 passed) — which are not part
  of the DST harness or corpus. The implementer's own EXIT annotation concedes the split
  ("composed-loop tests cover ... discrepancy + global halt"). EXIT text requires the
  discrepancy path in DST; no partial credit.

- C3 Dashboards and digest render from sim data (BUILD_PLAN.md:122): **PASS.** Executed:
  `cargo test -p fortuna-runner --test observability` → "test
  dashboards_and_digest_render_from_sim_data ... ok. 1 passed; 0 failed". Test is
  substantive (crates/fortuna-runner/tests/observability.rs:167): one sim run (mech_structural
  through fills + 3 settlements) → MetricsRegistry → Prometheus text assertions (incl.
  realized-PnL metric equals books' report) → a REAL axum server bound on 127.0.0.1 serving
  /api/boards (3 positions asserted) and /metrics (scrape byte-equals registry render) →
  daily digest composed from the same run's counters.

## Battery (review-procedure items, executed at HEAD 1a113ee in the clean worktree)

- `cargo fmt --check`: PASS — exit 0, empty diff.
- `cargo clippy --workspace --all-targets -- -D warnings`: PASS — "Finished `dev` profile
  [unoptimized + debuginfo] target(s) in 19.73s", exit 0.
- `cargo test --workspace` (DATABASE_URL=postgres://localhost/fortuna_dev): PASS — 515
  passed, 0 failed across all suites, exit 0. (Without DATABASE_URL the run is RED:
  fortuna-invariants i5_audit_append_only panics "DATABASE_URL must be set: EnvVar(NotPresent)"
  — sqlx::test env dependency, see note N1.)
- `scripts/run-dst.sh 3000`: PASS — "[dst] regression corpus: 0 seed(s)" / "[dst] OK: 0 corpus
  + 3000 random seeds, zero invariant violations", exit 0. Corpus contains zero committed
  regression seeds (consistent with no red seeds to date; the Phase 4 "regression seeds
  committed" item is not in this gate's rubric).

## Mechanical sweep (phase diff 77c4c65...c1e206f, added lines)

- unwrap/expect/panic/todo/unimplemented in gates/exec/state/venues/paper: 1 hit, a
  test-only `UtcTimestamp::parse_iso8601(...).unwrap()` in state tests. Not a money path. OK.
- SystemTime::now/Instant::now/Utc::now outside the core clock module: zero hits. OK.
- f32/f64 touching money/prices: `fee_multiplier: f64` (kalshi/dto.rs:322) is the venue's
  JSON double, converted at the boundary by `multiplier_decimal` (adapter.rs:1004) to
  `Decimal` with NaN/Infinity/negative rejection (tested, adapter.rs:1120-1135). Conversion
  boundary per house convention. OK.
- HashMap/HashSet iteration feeding bus/audit/sizing: zero added hits (new state uses
  BTreeMap/BTreeSet). OK.
- Test weakening (diff-wide): no `#[ignore`, no deleted assertions, no proptest case
  reductions, no loosened tolerances (19 deleted lines total in the diff, none asserts). OK.
- Secrets: KEY/TOKEN/SECRET hits are Kalshi doc-mirror placeholders under docs/research and
  doc comments; test RSA keys are generated at runtime (`RsaPrivateKey::new(&mut OsRng, ...)`,
  kalshi_auth.rs:34). No secret material in the repo. OK.
- GatedOrder discipline: struct has private fields with the single `pub(crate) assemble()`
  constructor in crates/fortuna-gates/src/order.rs; every out-of-crate construction site is a
  test helper running the REAL GatePipeline (e.g. fortuna-paper/tests/paper.rs:92). The new
  paper/kalshi `place` paths take GatedOrder. No bypass. OK.

## Findings

- [Critical — automatic-block rule] The phase diff touches the protected crate
  crates/fortuna-invariants/ (tests/i1_universal_gate.rs, tests/i3_runaway_halt.rs,
  tests/i4_killswitch_independence.rs; +1 line each). Hard rule: any diff touching this crate
  is an automatic BLOCK pending operator review, regardless of content. Content reviewed for
  the operator's convenience: each change is the identical additive fixture-field
  `volume_contracts: None,` in a sim-market constructor (compile-fix for the Market struct
  gaining a field in T1.3); no assertion logic, tolerance, or test name was modified, and all
  invariant tests pass (i1/i3/i4/i5 green in the 515-test run). Reproduction: `git diff
  77c4c65...c1e206f -- crates/fortuna-invariants/`.
- [Major] C1 unmet: "both mechanical strategies in Paper against recorded data streams" was
  never executed and cannot be executed — no recordings exist (fixtures/kalshi/ holds only
  README.md) and no runner→PaperVenue replay composition exists. Operator-blocked per
  GAPS.md:66-76 (quoted under C1) with exact unblock steps. UNVERIFIABLE → gate fails.
- [Major] C2 unmet: discrepancy paths are not exercised in DST. Evidence: zero `discrepanc`
  hits in crates/fortuna-core/tests/dst.rs; scripts/run-dst.sh runs only `cargo test -p
  fortuna-core --test dst`; the discrepancy/halt machinery is exercised solely in
  crates/fortuna-runner/tests/settlement_loop.rs:406-424 (composed-loop integration test,
  outside the DST harness). EXIT line (BUILD_PLAN.md:121-122) demonstrably requires it "in
  DST".
- [Minor] Operator's working tree carries uncommitted T2.2 WIP (fortuna-cognition signals.rs
  + tests, fortuna-ledger repos.rs/ledger.rs edits) that fails `cargo fmt --check` (exit 1,
  4 files) and breaks `cargo clippy` compilation of fortuna-ledger (24 errors, E0282 at
  repos.rs:809 sqlx::query!). Not part of the phase diff; battery for this gate was run in a
  clean worktree at the same HEAD. Flagged so the dirty state is not mistaken for Phase 1
  regression.

## Notes (not findings)

- N1: fortuna-invariants/tests/i5_audit_append_only.rs uses #[sqlx::test] and hard-fails
  without DATABASE_URL ("DATABASE_URL must be set: EnvVar(NotPresent)"). Green against local
  Postgres (fortuna_dev). Standard sqlx practice; recorded so a future red run without the
  env var is diagnosed as environmental.
- N2: dst-corpus has 0 committed regression seeds — consistent with zero red seeds to date,
  per dst-corpus/README.md policy. Becomes a gate item only at Phase 4 (PROMPT.md acceptance:
  "regression seeds committed").

## Commands run (verbatim verdict lines)

```
# operator working tree (dirty, HEAD 1a113ee) — contaminated, reported as F4
cargo fmt --check                      -> FMT-EXIT:1 (136 diff lines; all 4 files are uncommitted T2.2 WIP)
cargo clippy --workspace --all-targets -- -D warnings
                                       -> "error: could not compile `fortuna-ledger` (lib) due to 24 previous errors"

# clean scratch worktree at the SAME HEAD 1a113ee (/tmp/fortuna-gate-head) — gate evidence
cargo fmt --check                      -> FMT-EXIT:0 (empty)
cargo clippy --workspace --all-targets -- -D warnings
                                       -> "Finished `dev` profile [unoptimized + debuginfo] target(s) in 19.73s" / CLIPPY-EXIT:0
cargo test --workspace                 -> FAILED: i5_audit_append_only "DATABASE_URL must be set: EnvVar(NotPresent)" / TEST-EXIT:101
DATABASE_URL=postgres://localhost/fortuna_dev cargo test --workspace
                                       -> 515 passed; 0 failed (sum over all suites) / TEST-EXIT:0
./scripts/run-dst.sh 3000              -> "[dst] regression corpus: 0 seed(s)"
                                          "[dst] master seed 1781106172459 -> 3000 random scenario(s)"
                                          "[dst] OK: 0 corpus + 3000 random seeds, zero invariant violations" / DST-EXIT:0
cargo test -p fortuna-runner --test observability
                                       -> "test dashboards_and_digest_render_from_sim_data ... ok" / 1 passed; 0 failed
cargo test -p fortuna-runner --test sim_loop --test mech_extremes
                                       -> sim_loop: 7 passed; 0 failed / mech_extremes: 15 passed; 0 failed
cargo test -p fortuna-paper            -> tests/paper.rs: 11 passed; 0 failed
cargo test -p fortuna-runner --test settlement_loop --test veto_loop
                                       -> settlement_loop: 8 passed; 0 failed / veto_loop: 8 passed; 0 failed
```

## Gate result

Phase 1 EXIT: **NOT PASSED** (C1 UNVERIFIABLE/operator-blocked, C2 FAIL, C3 PASS; the gate
requires every item PASS). Overall verdict **BLOCK** — additionally automatic per the
protected-crate rule. Operator paths forward: (1) adjudicate the fortuna-invariants
fixture-field touch (content is mechanical and additive); (2) record the Kalshi
websocket/book/trade fixtures per GAPS.md:66-76 to unblock C1; (3) either add
discrepancy-path arms to the DST harness or amend the EXIT contract — the verifier does not
negotiate which.
