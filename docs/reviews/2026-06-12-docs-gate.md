# Review: DOCS GATE (documentation-plan W1–W4 set) — 2026-06-12
Base: 5120db8 (docs pinned at 334612d; HEAD moved to cc099b4 mid-review — design-doc-only, zero source drift)
Head: working tree (uncommitted: README.md, AGENTS.md, docs/{quickstart,architecture,verification,operations}.md, docs/runbooks/*.md ×8, docs/design/fortuna-cli.md edit)
Verdict: BLOCK (2 unledgered Majors + 1 ledgered Major; all small, enumerated, re-gate cheap)
Protected crate touched: no (doc-only diff; `git status` confirms no crates/ paths)

## Criteria (fixed before reading the diff; rubric = operator brief A–D + documentation-plan STYLE CONTRACT)

- C1 Quickstart executes literally end-to-end (brief A): **PASS** — every
  non-OPERATOR-RUN command executed with the documented outcome:
  `createdb fortuna_dev || true` exit 0; `cargo build --release -p fortuna-live
  -p fortuna-cli` exit 0, 54s, both binaries (scratch CARGO_TARGET_DIR per disk
  rule); guarded config/.env cps behaved as documented (config/fortuna.toml
  created, gitignored per .gitignore:4; .env untouched); `set -a && source .env
  && set +a` clean; battery: fmt exit 0, clippy -D warnings exit 0,
  `cargo test --workspace` green (every suite "test result: ok", zero
  error/FAILED lines, task exit 0), `scripts/run-dst.sh` exit 0 — "[dst] OK: 3
  corpus + 2000 random seeds, zero invariant violations", synthesis 2000 ok,
  settlement 2000 ok (exactly the eleven documented fault arms), daemon_smoke
  15/0. §5 OPERATOR-RUN marker is correctly placed (secrets + dead-man arming +
  soak clock); §6/§7 REVIEW-VERIFIED: all five boot lines verbatim at
  fortuna-live main.rs:89/91,103,117,194,246 in documented order; routes
  /rota + /api/rota/v1/* GET-only with the 405 route-table test
  (fortuna-ops/tests/dashboard.rs:86,157); stop semantics match
  fortuna-cli main.rs:636–767.
- C2 Runbook non-OPERATOR-JUDGMENT commands execute (brief B): **FAIL (one
  command)** — executed green: `fortuna status` (byte-identical to the
  operations.md §1.2 captured example), `fortuna config check` both paths
  ("config OK: …", exit 0), `fortuna logs daemon` error path ("has `fortuna
  start` run?", exit 1), `scripts/killswitch-test.sh` (byte-identical success
  block, "self-test OK: cancelled 2/2 orders, reported 1 positions", exit 0),
  `git for-each-ref refs/original/` (prints refs/original/refs/heads/main =
  still pending, exactly as documented), `git check-ignore -v .keys .env data`
  (all three resolve), `git status --ignored` (!! .env/.keys/data), both
  fixture secret-sweeps (exit 1 = clean, as documented). RED:
  backup-restore.md §2 `pg_dump` — see Finding 1.
- C3 OPERATOR-JUDGMENT steps match committed code/scripts; marking calibrated
  (brief B): **PASS** — halt/rearm/kill syntax matches fortuna-cli main.rs
  (usage, required --reason/--operator, output strings at :1072/:1085;
  kill→standalone binary at :1000–1031, --flatten→"report" at :1007, journal
  default :1006; killswitch surface main.rs:1–11; unwired-venue exit 3 at
  killswitch main.rs:76); recorder sessions match the example recorders' env
  names and demo-host rails; demo-flip §3 explicitly REVIEW-VERIFIED-not-
  executable and the boot refusal string is verbatim at boot.rs:246. No
  dangerous step unmarked; no trivial step over-marked.
- C4 soak-start.md vs FINAL_REPORT.md §5 (brief C): **FAIL (divergent)** — see
  Finding 2. The runbook matches the soak-go gate's OPERATOR START COMMAND
  (raw `./target/release/fortuna-live config/fortuna.toml`, soak-go-gate
  lines 202–215) and reality (unmanaged recorder pid 79813 running ⇒ managed
  `fortuna start` refuses today; `target/release/` binaries absent as claimed);
  FINAL_REPORT §5 diverges on config copy (`cp` clobbers vs `cp -n`), start
  command priority (managed first, refusal undisclosed), and the M3 forward
  promise. RECOMMENDATION (per brief): FINAL_REPORT §5 collapses to the
  acceptance record + pointer to docs/runbooks/soak-start.md as the living
  procedure; the runbook keeps citing the GO verdict. GAPS.md (5120db8) already
  ledgers this same reconciliation direction.
- C5 Claim tracing, ≥10 sampled claims per doc (brief D): **PASS with four
  broken citations (Findings 4–5)** — the named tally reproduces exactly:
  `grep -l "Verdict:" docs/reviews/*.md | wc -l` = 32;
  `grep "Verdict:" | wc -l` = 34; leading-token classification = 17 BLOCK /
  14 ACCEPT-WITH-GAPS / 3 ACCEPT, exactly as verification.md §4.7 states.
  Traced true (sample): five boot lines; env contract + PLACEHOLDER_MARKS
  (boot.rs:57–73,106–115); .cargo/config.toml DATABASE_URL default
  (force=false); rust-toolchain pin; 15 crates; killswitch deps = core+venues
  only (Cargo.toml); deny(unwrap_used) heads in gates/exec/state/venues/paper;
  GatedOrder Serialize-not-Deserialize (pipeline.rs:99) + 3 compile_fail
  doctests; i1–i7 one file per invariant; i4 spawns the real binary; i7
  "system cannot promote"; CLI fn/line cites (main.rs:56,58–66,210,302,470,
  561,607,676,744–748,757,880,902,962,1034,1061–1086) all exact; clean-shutdown
  line at fortuna-live main.rs:284; config example cites :58–61,:104–105,
  :110,:111 exact; slack.rs:24–33,185–196; connect_readonly_pool 2-conn/3s
  (ledger lib.rs:81–88) wired at main.rs:125; rota routes + audit-cursor +
  scan_recorder + fmtCents at cited lines; views.rs null-not-zero claims;
  run-dst.sh four stages/default 2000/DST_MASTER_SEED read by all three
  harnesses/replay.sh --seed; 304f746 contents; 803/0/0 and 10k-DST quotes in
  soak-go-gate; 940/0/0 in track-c-regate; war-story cites (flood,
  halts_applied:4 probe, three-boundary fix, INDEPENDENT F1 with spec quote,
  M1 example-config pin) all present in the cited verdicts; all 13 cited
  verdict files + 5 design docs + R12 screenshots + fixture session records
  exist; GAPS purge-quote verbatim (GAPS:1482–1495); "23 relations owned by
  fortuna_app" reproduced live (=23); A9 exit-0 pin and bus item 5 resolve;
  BUILD_PLAN T4.4 ticked with the recorder-refusal contract.
- C6 STYLE CONTRACT conformance: **FAIL (two violations)** — audience line ✓
  all 12 docs; cross-links ✓; vocabulary ✓; point-don't-duplicate ✓; honest
  status pinned to 334612d ✓ EXCEPT operations.md §3 "is running" (Finding 3)
  and the operations.md header branch claim (Finding 6).
- C7 Scope discipline: **PASS with note** — docs writers stayed in lane; the
  BUILD_PLAN/GAPS working-tree edits observed at review start belonged to a
  concurrent Track-A session (since committed). Of W4's three surgical fixes
  only fortuna-cli.md A1 is in this set (Finding 7).
- C8 No secrets; nothing dangerous unmarked: **PASS** — env NAMES only across
  the set; fixture sweeps clean; OPERATOR-RUN/JUDGMENT markers cover every
  consequential step.
- C9 Material drift since pin 334612d: **PASS** — `git diff 334612d..cc099b4`
  touches only FINAL_REPORT.md, GAPS.md, and a new design spec; zero source
  drift; crate count still 15.

## Findings

- [Major] backup-restore.md §2: the pg_dump command FAILS as written on this
  machine — reproduction: `pg_dump --format=custom --file=... "$DATABASE_URL"`
  → "pg_dump: error: aborting because of server version mismatch / server
  version: 16.13 (Homebrew); pg_dump version: 15.14 (Homebrew)", exit 1,
  zero-byte dump. §4 declares §2 "the entire backup story"; the doc's
  "syntax verified against pg_dump 15.14 on this machine" is verified-but-
  never-executed — the docs-gate pathology itself. The identical command with
  /opt/homebrew/opt/postgresql@16/bin/pg_dump succeeds (54KB dump produced and
  removed). Fix: version-matched binary path (or `$(brew --prefix
  postgresql@16)/bin/pg_dump`) + actually execute it once.
- [Major] The two start contracts diverge (brief C) — reproduction: diff of
  soak-start.md §§1–4 vs FINAL_REPORT.md §5 Steps 0–3 + soak-go-gate
  lines 202–215 + live process table (unmanaged recorder pid 79813).
  Divergences: (a) §5 Step 1 `cp` clobbers an existing operator config; the
  runbook's `cp -n` does not; (b) §5 Step 3 leads with managed `fortuna
  start`, which refuses on this machine today and is not the gate-verified
  OPERATOR START COMMAND the runbook pins; the refusal is undisclosed in §5;
  (c) §5 Step 6 promises "[M3 … lands before the first soak halt drill]" while
  halt-and-rearm.md documents M3 open ("this runbook is the notice") and the
  bus/GAPS now mark it boundary-blocked. LEDGERED: GAPS (5120db8) already
  proposes §5 → pointer-to-runbook; this gate concurs — FINAL_REPORT §5
  becomes the acceptance record pointing at soak-start.md; the runbook cites
  the GO verdict (it already does).
- [Major] operations.md §3 opening: "The Phase-4 EXIT soak … is running per
  FINAL_REPORT.md §5" — executed evidence: no fortuna-live process; curl
  127.0.0.1:9187 → connection refused (exit 7); docs/reviews/soak-log.md does
  not exist. The soak has NOT started (the GO verdict licenses "fit to start
  NOW"). Aspirational present tense, banned by the binding STYLE CONTRACT.
  (Charitable reading "is run per" noted; the sentence as written asserts a
  live state the operator's own console disproves.) Fix: one sentence.
- [Minor] Wrong-ledger citations (4): troubleshooting §4 "(ASSUMPTIONS.md,
  T4.4 battery environment note)" — no such entry (substance true via
  .cargo/config.toml force=false); troubleshooting §9 "(ASSUMPTIONS.md, T4.4
  slice 3)" and soak-start §4 "(ASSUMPTIONS.md, T4.4 slice 2)" — the T4.4
  slice taxonomy lives in GAPS.md:1252, not ASSUMPTIONS; troubleshooting §10
  "(ASSUMPTIONS.md disk incident …)" — the ENOSPC incident is GAPS.md:246
  (the ~8GB-per-track figure itself is fair per GAPS.md:1440).
- [Minor] operations.md §1.3 example transcript with concrete pids
  (81234/81237) is attributed to "the manual smoke recorded in
  fortuna-cli.md §13" — §13 contains the procedure and expected shapes, no
  transcript; the pids are untraceable. Output FORMATS verified true against
  main.rs:561,607 — behaviorally accurate, provenance is not. Label
  illustrative or drop the pids.
- [Minor] operations.md header: "branch `build/phase-0`" — `git branch
  --contains 334612d` = main only; build/phase-0 sits at eab031e.
- [Minor] Plan-vs-delivery: of W4's three surgical staleness fixes only the
  fortuna-cli.md A1 cross-ref is in this set (its cited tests verified to
  exist: daemon_smoke.rs:121,215; shutdown.rs:50); the FINAL_REPORT fixtures
  note landed via concurrent 5120db8; no PROMPT.md edit exists anywhere (no
  stale start/stop names found at HEAD — item likely moot, but unaccounted).
- [Info] kill-switch-drill.md quotes CLAUDE.md's I4 (which includes
  "Postgres") and co-cites "spec Section 3" — spec §3's I4 sentence omits
  Postgres (no-Postgres is the Principle-9 exception). Quote accurate to
  CLAUDE.md; dual attribution loose.
- [Info] FINAL_REPORT §6 Step 0 names `KALSHI_PRIVATE_KEY` vs .env.example's
  `KALSHI_PRIVATE_KEY_PATH` — pre-existing staleness outside this diff;
  sweep it when §5 is collapsed.
- [Info] verification.md §4.5's red seed 11819682492387934495 lives on
  track-c (crates/fortuna-state/tests/perp_dst.rs), not main's corpus (3
  anchors only) — consistent with the doc's own §5 disclosure that the perp
  harness rides track-c.

## Commands run (verbatim verdict lines; scratch CARGO_TARGET_DIR=/tmp/fortuna-docs-gate, cleaned after)

    createdb fortuna_dev || true                       # exit 0 (already exists)
    cargo build --release -p fortuna-live -p fortuna-cli   # Finished `release` … 54.16s
    cargo fmt --check                                  # exit 0
    cargo clippy --workspace --all-targets -- -D warnings  # exit 0, Finished 28.26s
    cargo test --workspace                             # all suites "test result: ok"; 0 failed; 0 error/FAILED lines
    scripts/run-dst.sh                                 # exit 0; "[dst] OK: 3 corpus + 2000 random seeds, zero invariant violations";
                                                       # synthesis 2000 ok; settlement 2000 ok (11 arms); daemon_smoke 15/0
    scripts/killswitch-test.sh                         # exit 0; "self-test OK: cancelled 2/2 orders, reported 1 positions"; PASS line verbatim
    ./fortuna status                                   # byte-identical to operations.md §1.2 example; exit 0
    ./fortuna config check [+ --config-path example]   # "config OK: …"; exit 0 both
    ./fortuna logs daemon                              # documented error text; exit 1
    git for-each-ref --format='%(refname)' refs/original/  # refs/original/refs/heads/main (pending, as documented)
    git check-ignore -v .keys .env data                # all resolve; git status --ignored shows !! all three
    grep -rn "PRIVATE KEY" fixtures/ ; grep -rln '"headers"' fixtures/   # both exit 1 (clean, as documented)
    pg_dump --format=custom --file=data/backups/… "$DATABASE_URL"        # FAILS: server 16.13 vs pg_dump 15.14, exit 1  ← Finding 1
    /opt/homebrew/opt/postgresql@16/bin/pg_dump … (same args)            # exit 0, 54262-byte dump (probe; removed)
    grep -l "Verdict:" docs/reviews/*.md | wc -l       # 32;  grep "Verdict:" | wc -l → 34; classified 17/14/3 (reproduces §4.7)
    curl -s -m 3 http://127.0.0.1:9187/api/rota/v1/health  # connection refused (exit 7) ← Finding 3 evidence
    git diff 334612d..cc099b4 --stat                   # FINAL_REPORT.md + GAPS.md + 1 design doc; zero source drift

Tree restored post-review: /tmp/fortuna-docs-gate removed; quickstart-created
config/fortuna.toml and data/backups/ removed (absent pre-review); no repo
file modified by this gate other than this verdict.
