# Review: Task B3 — 2026-06-18
Base: 5e2b170  Head: c4dec57  Verdict: ACCEPT
Protected crate touched: no

## Criteria (fixed before reading the diff)

Per the task brief and FORTUNA spec:

- C1 `DecisionCycle::set_calibration` / `is_calibrated` (Part 1): PASS — evidence: `cargo test -p fortuna-cognition --test cycle -- set_calibration` → 2 passed
- C2 `SynthesisStrategy::set_calibration` delegates to `cycle.set_calibration`, returns `true` (Part 1): PASS — code inspection synthesis.rs:331-334 + cycle test
- C3 `SimRunner::refresh_synthesis_calibration` finds synthesis arm via `set_calibration`, updates `calibration_quality` (Part 1): PASS — runner.rs:2968-2991
- C4 `ActiveRunner::refresh_synthesis_calibration` delegates across Sim/Kalshi/PaperLive (Part 1): PASS — daemon.rs:1122-1133
- C5 Daemon per-segment calibration refresh via `calibration_for_scope`, guarded by `syn.category.as_deref()`, fetch-failure-keeps-last-known posture (Part 1): PASS — daemon.rs:2636-2664
- C6 `on_event` skips `cycle.run` when `!is_calibrated()`, increments `cold_calibration_skips`, non-silent (Part 2/F5): PASS — synthesis.rs:557-573
- C7 No-deadlock reasoning present as code comment (Part 2): PASS — synthesis.rs:558-573 (7-line comment with full reasoning)
- C8 No `panic!`/`unwrap`/`expect` in production code (gates/exec/state): PASS — sweep returned no production-code hits
- C9 No `SystemTime::now`/`Instant::now`/`Utc::now` outside clock module: PASS — sweep clean
- C10 `fortuna-runner` and `fortuna-cognition` gained no Postgres dep (fetch is daemon-side): PASS — Cargo.toml for both crates has no sqlx/postgres
- C11 Test integrity — `drive_drains_and_persists_the_synthesis_arms_beliefs`: PASS — only additions (calibration_params seed row, category="weather" config); final assertion `after >= 1` unchanged
- C12 Test integrity — `discovery_market_back_auto_confirms_and_synthesis_drafts_a_belief`: PASS — only additions (calibration_params seed, category="weather"); all assertions (a)/(b)/(c) unchanged verbatim
- C13 New tests are non-vacuous (cold: mind=0 calls; warm: mind>=1 calls): PASS — `cold_arm_calls_mind_zero_times_and_increments_skip_counter` and `refresh_synthesis_calibration_warms_the_arm_and_mind_gets_called` both pass; CountingMind proves zero/nonzero calls
- C14 `cold_calibration_skips` surfaced in `RunCounters` (non-silent): PASS — runner.rs:355, aggregated at runner.rs:2893
- C15 DST clean: PASS — `bash scripts/run-dst.sh` completed exit 0 (set -euo pipefail; last suite ingest_dst 5/5 pass)
- C16 Fmt clean: PASS — `cargo fmt --all --check` exit 0
- C17 Clippy clean: PASS — `cargo clippy -p fortuna-cognition -p fortuna-runner -p fortuna-live --all-targets -- -D warnings` finished with no warnings

## Findings

- [Minor] `refresh_synthesis_calibration` uses two different predicates: `set_calibration() == true` to detect synthesis arms (and set `found`), but `edge_count().is_some()` to update `calibration_quality`. Both are currently satisfied only by `SynthesisStrategy`, so no functional defect today. If a future strategy implements one but not the other, quality and calibration could diverge silently. Recommend aligning both guards on `set_calibration() == true` or documenting the intentional separation — file: crates/fortuna-runner/src/runner.rs:2973-2990.

## Commands run (verbatim results)

```
git diff --name-only 5e2b170..c4dec57 | grep fortuna-invariants
(empty — protected crate not touched)

cargo fmt --all --check
FMT EXIT: 0

SQLX_OFFLINE=true cargo clippy -p fortuna-cognition -p fortuna-runner -p fortuna-live --all-targets -- -D warnings
Finished `dev` profile [unoptimized + debuginfo] target(s) in 0.73s
(no warnings)

SQLX_OFFLINE=true cargo test -p fortuna-cognition -p fortuna-runner -p fortuna-live
[67+7+3+5+21+1+4+14+1+8+9+7+16+11+8+19+14+15+1+2+10+14+1+12+10+9+4+7+4+54+7+9+16+48+0+3+6+6+1+14+11+6+5+29+2+1+9+1+10+5+1+2+8+2+9+2+2+16+9+13+1+1+16+2+2+15+1+1+14+2+48+1+7+3+2+1+12+11+2+1+16+8+0+0+0 total]: all passed
778 tests, all ok

SQLX_OFFLINE=true cargo test -p fortuna-cognition --test cycle -- set_calibration
running 2 tests: set_calibration_some_makes_is_calibrated_true ... ok, ..._none_false ... ok

SQLX_OFFLINE=true cargo test -p fortuna-runner --test synthesis_loop -- cold_arm
running 1 test: cold_arm_calls_mind_zero_times_and_increments_skip_counter ... ok

SQLX_OFFLINE=true cargo test -p fortuna-runner --test synthesis_loop -- refresh_synthesis_calibration
running 1 test: refresh_synthesis_calibration_warms_the_arm_and_mind_gets_called ... ok

SQLX_OFFLINE=true bash scripts/run-dst.sh
[ingest-dst] 429 storm: quarantined + loud alert ✓
[ingest-dst] timeout: degraded weather, macro ingested ✓
[ingest-dst] quarantine -> rearm -> ingesting ✓
[ingest-dst] crash+rebuild: resumed cleanly ✓
[ingest-dst] burst: 10 accepted / 90 refused (over-volume) ✓
test result: ok. 5 passed; 0 failed (last suite; set -euo pipefail confirms no prior failures)
```

## Notes

The comment deleted from `drive_drains_and_persists_the_synthesis_arms_beliefs`
("calibration is irrelevant to belief DRAFTING — it gates only pricing") was
factually incorrect after B3: the paid Mind call is now gated on `is_calibrated()`,
and the Mind must run before a belief can be drafted. Removing the incorrect comment
is correct hygiene, not test weakening.
