# Review: T5.B7 slice 1a (prob_claims/v1 scalar-belief foundation + swappable scoring) — 2026-06-13
Base: main  Head: b6ed374 (track-c, UNMERGED worktree /Users/xavierbriggs/fortuna-wt-c)  Verdict: ACCEPT-SLICE
Protected crate touched: NO (crates/fortuna-invariants diff EMPTY; verified)

## Criteria (fixed before reading the diff — from task contract A–F + design §1 + CLAUDE.md)
- A SCORING MATH CORRECTNESS (CrpsPinballRule = mean pinball = discretized CRPS, proper; BrierRule = (p−1{happened})²; tests prove properness non-vacuously): PASS — impl scoring.rs:360-379 (pinball), :294 (Brier); tests/scoring.rs exact vectors (:398,:407,:449), median-optimal proptest (:579), 1-quantile degenerate REJECTED (:417). Independent /tmp recompute reproduced the documented constants (0.0667/0.7333) and the properness direction. MUTATION (code-level, disk OK): sign-flipped pinball_loss in-place → 4 tests RED (proper_scoring_median_optimal + 3 exact-vector); restored + cargo clean -p (artifact evicted).
- B VALIDATION (binary p strictly (0,1); categorical sum≈1 within tol; scalar ≥2 quantiles, q strictly↑ in (0,1), v non-decreasing, all finite; deny_unknown_fields): PASS — validate_binary_p :389, validate_categorical :402, validate_scalar :434; deny_unknown_fields on both envelopes (:118,:177) and sub-types (:82,:95). Each rejection exercised non-vacuously (crossing-v REFUSED tests/scoring.rs:226,:540; q-at-0/1 :195/:201; sum-outside-tol :120).
- C I5 / IMMUTABILITY + replay-shape (durable facts carry no mutable score state; re-score deterministic): PASS — PredictiveDistribution/RealizedOutcome are plain enums, no cached score, no interior mutability; score() is &self/&pred/&outcome→fresh f64; determinism tests assert bit-identical to_bits() (tests/scoring.rs:552,:562).
- D HOUSE STYLE (f64 only for forecast/probability, never money; no unwrap/expect/panic outside cfg(test); thiserror ScoreError; no f32): PASS — no money types in f64 (only doc-comment boundary note scoring.rs:18-20); no unwrap/expect/panic in src; four unreachable!() are provably-dead arms guarded by upstream applies_to/kind-parity returns (267/272/338/343), NOT a money path; thiserror ScoreError :45; no f32 anywhere.
- E PARALLEL-MODULE + PROTECTED CRATE (beliefs.rs untouched; invariants EMPTY; no test weakening): PASS — git diff main...HEAD beliefs.rs EMPTY; invariants EMPTY; lib.rs = single additive `pub mod scoring;`; diff-wide weakening sweep clean (no #[ignore], no proptest cases reduction, no removed assert/tolerance); existing beliefs.rs binary tests still 8/8 green.
- F BATTERY (fmt/clippy -D warnings/test incl. scoring count): PASS — fmt exit 0; clippy exit 0 "Finished" no diagnostics; full fortuna-cognition suite green, scoring target 54 passed/0 failed/0 ignored.

## Findings
- [Minor/observation, not blocking] Four `unreachable!()` in scoring.rs (282,292,353,357) — defensible (provably-dead arms after upstream guards; cognition is not a money path), idiomatic; no action required.
- No Critical, Major findings. Out-of-slice files in the diff (fortuna-core perp.rs +106, funding_window.rs +192) are the pre-existing FundingWindow kernel (design §2.3), graded only for non-weakening / no f64-money: both clean (PerpPrice(i64), no #[ignore]).

## Commands run (verbatim results, trimmed)
- cargo fmt -p fortuna-cognition --check → exit 0
- cargo clippy -p fortuna-cognition --all-targets -- -D warnings → Finished `dev` in 2m23s, exit 0, no warnings
- cargo test -p fortuna-cognition --test scoring → test result: ok. 54 passed; 0 failed; 0 ignored
- cargo test -p fortuna-cognition (full) → all 14 targets ok; beliefs 8, calibration 9, ..., scoring 54, veto 16; 0 failed; 0 ignored
- MUTATION (sign-flip pinball_loss) → test result: FAILED. 50 passed; 4 failed (crps_grid_y_zero, crps_grid_y_two, crps_realistic_funding_rate_vector, proper_scoring_median_optimal); RESTORED via git checkout; cargo clean -p fortuna-cognition removed 16692 files/1.7GiB; scoring.rs git-clean; disk 15Gi
