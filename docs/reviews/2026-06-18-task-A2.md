# Review: Task A2 — Persist paper fills with strategy from the daemon (F12)
Base: bc5f44d  Head: 1aee7ff  Verdict: ACCEPT-WITH-GAPS
Protected crate touched: no

## Criteria (fixed before reading the diff)

- C1 fortuna-runner has NO new fortuna-ledger/sqlx/Postgres dependency (Cargo.toml unchanged): PASS — evidence: `cat crates/fortuna-runner/Cargo.toml` shows no ledger/sqlx entry; `git diff bc5f44d..1aee7ff -- 'crates/fortuna-runner/Cargo.toml'` produces no output.

- C2 `pending_fills: Vec<(Fill, Option<StrategyId>)>` field added, initialized in BOTH `new_with_journal` and `new_with_venue` constructors: PASS — evidence: field at runner.rs:156; `new_with_venue` init at runner.rs:551; `new_with_journal` delegates to `new_with_venue` at runner.rs:457, so both paths are covered.

- C3 Strategy resolved BEFORE `release_if_terminal` in `drain_fills` `if app.applied` block: PASS — evidence: runner.rs:1522-1527 shows strategy resolution via `self.manager.intent(app.intent).map(|r| r.order.strategy.clone())` and `push` at line 1526, then `release_if_terminal` at line 1527.

- C4 `pub fn drain_applied_fills(&mut self) -> Vec<(Fill, Option<StrategyId>)>` uses `std::mem::take`: PASS — evidence: runner.rs:693 `std::mem::take(&mut self.pending_fills)`.

- C5 Daemon drains per segment via `FillsRepo::insert(venue, &fill, None, strategy.as_deref())` using ledger pool, NOT gated on `synthesis_refresh`: PASS — evidence: daemon.rs:2559-2601; the block is outside the `synthesis_refresh` block with explicit comment; the `if let Some(fpool) = &fills_pool` guard is separate.

- C6 `producer` passed as `None` (deferred to D4); no producer fields added to core types: PASS — evidence: daemon.rs:2585 `None`; diff shows no additions to OrderSnapshot, CandidateOrder, or Fill struct fields.

- C7 Error on fill persist does NOT crash daemon loop AND is NOT silently swallowed: PASS — evidence: daemon.rs:2591-2597, `Err(e)` pushes to `alerts` vec; `alerts` is flushed at end of segment via `runner.route_alerts(slack, &alerts)` at daemon.rs:3145.

- C8 Runner unit test asserts `drain_applied_fills` returns fill with correct strategy, second drain empty: PASS — evidence: `fill_drain.rs:168-171` asserts `Some("mech_structural")`; line 177 asserts `fills2.is_empty()`. Test passed in session.

- C9 Daemon integration test asserts fills row exists with strategy, producer NULL, and idempotency on replay: PASS (with gap) — evidence: `fill_persist.rs:157-169` asserts `fill_count >= 1` and `bad_rows == 0` (strategy non-null, producer null); idempotency asserts `Ok(false)` return and unchanged count. Gap: test uses `>= 1` not exactly 1 row per brief (Minor).

- C10 Idempotency assertion is genuine, not vacuous: PASS — evidence: test reads actual `fill_id` from DB, constructs Fill with that id, calls `FillsRepo::insert`, asserts `!inserted` (would RED if `ON CONFLICT` were removed because insert would return `true`).

- C11 Protected crate untouched: PASS — evidence: `git diff --name-only bc5f44d..1aee7ff | grep fortuna-invariants` produced no output.

- C12 No test weakening (deleted asserts, #[ignore], loosened tolerances): PASS — evidence: `grep '^-' diff | grep -E 'assert|#\[ignore'` produced no output.

## Findings

- [Minor] daemon integration test asserts `fill_count >= 1` rather than the exact count the brief specifies ("exactly one fills row"). The spirit is met (fills land + idempotency holds) but the letter of the brief is looser. Reproduction: read fill_persist.rs:157. No code behavior is wrong; test could be strengthened by asserting the count equals the number of expected arb fills (3 legs → 3 fills).

- [Pre-existing, not A2] `forecasts_scorecard_aggregates_resolved_scores_per_producer` in `fortuna-ops --test rota` fails with `CorruptRow { scalar_belief ... not resolvable }`. Confirmed identical failure on base commit bc5f44d — not introduced by A2.

## Commands run (verbatim results)

```
cargo fmt --all --check
→ (no output, clean)

SQLX_OFFLINE=true cargo clippy -p fortuna-runner -p fortuna-live --all-targets -- -D warnings
→ Finished `dev` profile [unoptimized + debuginfo] target(s) in 9.93s (no warnings, no errors)

SQLX_OFFLINE=true cargo test -p fortuna-runner -p fortuna-live 2>&1 | grep 'fill_drain\|fill_persist\|result'
→ Running tests/fill_persist.rs → test result: ok. 1 passed; 0 failed
→ Running tests/fill_drain.rs   → test result: ok. 1 passed; 0 failed
→ All runner/live test suites: 0 failed

SQLX_OFFLINE=true bash scripts/run-dst.sh
→ [dst] OK: 15 corpus + 2000 random seeds, zero invariant violations

git diff --name-only bc5f44d..1aee7ff | grep fortuna-invariants
→ (no output)

Workspace test:
→ FAILED: forecasts_scorecard_aggregates_resolved_scores_per_producer (fortuna-ops/tests/rota.rs)
→ Confirmed pre-existing at bc5f44d: identical CorruptRow failure before A2
```
