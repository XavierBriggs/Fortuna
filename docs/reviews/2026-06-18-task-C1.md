# Review: C1 — Venue-neutral MarketView (bracket geometry) + decouple weather path from KalshiMarket
Base: c4dec57  Head: 01c3f41  Verdict: ACCEPT-WITH-GAPS
Protected crate touched: no

## Criteria (fixed before reading the diff)

- C1a. Decoupling: `grep -rn "kalshi::dto\|KalshiMarket\|KalshiMarketStatus" crates/fortuna-live/src/` returns nothing (spec Area 5 / brief goal): PASS — command returned empty, exit=1.
- C1b. `MarketView` extended with `strike_type/floor_strike/cap_strike/status` fields (brief §1): PASS — discovery.rs adds all four fields, all correctly typed.
- C1c. `market_to_bucket(&MarketView)` — correct arithmetic (between→InRange, greater→floor+1, less→cap-1, unknown→None) (brief §2): PASS — implementation matches spec, confirmed by golden regression test.
- C1d. `KalshiMarket→MarketView` conversion in `kalshi::` adapter; `WeatherMarketSource::day_set` returns `Vec<MarketView>` (brief §3–4): PASS — `kalshi_market_to_market_view` in weather.rs; trait moved to `weather_source.rs`; `KalshiWeatherSource::day_set` returns `Vec<MarketView>`.
- C1e. `WeatherMarketSource` trait moved to venue-neutral module; `fortuna-live` imports from `fortuna_venues::WeatherMarketSource` (brief §4): PASS — `crates/fortuna-venues/src/weather_source.rs` exists; `daemon.rs:1650` and `main.rs` import from `fortuna_venues::WeatherMarketSource`.
- C1f. Daemon filter uses string `m.status == "active"` (brief §5): PASS — `daemon.rs:2474` confirmed.
- C1g. Golden regression test covers all 4 strike_type cases with exact bucket values (brief tests): PASS — `golden_regression_market_view_geometry` in `aeolus_bucket_match.rs`; covers less/between/greater/unknown-str/None-type; exact `BucketKind` values asserted.
- C1h. No existing test assertion weakened to accommodate the refactor (test-weakening sweep): ACCEPT-WITH-GAPS — two deletions found (see findings below).
- C1i. cargo fmt clean: PASS — `fmt_exit=0`.
- C1j. clippy -D warnings clean (fortuna-cognition, fortuna-venues, fortuna-live): PASS — `Finished dev profile — no warnings or errors`.
- C1k. DST corpus zero invariant violations: PASS — `[dst] OK: 15 corpus + 2000 random seeds, zero invariant violations`.
- C1l. No `panic!/unwrap/expect` in money/gate paths (mechanical sweep): PASS — all `expect` hits are in test code or display-only title fallback; `unwrap_or_else` in `kalshi_market_to_market_view` is a display title field, not a money path.
- C1m. Protected crate `crates/fortuna-invariants/` not touched: PASS — `git diff --name-only c4dec57..01c3f41 | grep fortuna-invariants` returned empty.
- C1n. No new `f64/f32` in money paths, no `SystemTime::now` outside clock impls, no `HashMap` nondeterminism in bus/audit paths: PASS — sweep returned empty for all three patterns in added lines.
- C1o. No circular dependency (fortuna-venues → fortuna-cognition, cognition has no dep on venues): PASS — confirmed via `cargo metadata`.

## Findings

- [Minor] One existing test assertion deleted without equivalent coverage: `assert!(!event_grades_on("KXHIGHNY-26JUN13", "2027-06-13"))` was dropped from `kalshi_weather_source.rs::event_grades_on_rejects_other_dates_and_malformed_input`. Replaced with `assert!(!event_grades_on("KXHIGHNY-26JUN13", "2026-06-13-extra"))` (4-component case). The year-discrimination behavior is still correct by implementation (2027→"27JUN13"≠"26JUN13") and the 4-component replacement is new positive coverage, but the year-mismatch case is no longer explicitly asserted. Reproduction: write `assert!(!event_grades_on("KXHIGHNY-26JUN13", "2027-06-13"))` and it passes — no logic regression, only missing explicit coverage.
  File: `/Users/xavierbriggs/fortuna/crates/fortuna-venues/tests/kalshi_weather_source.rs:54`

- [Minor] Test doc comment in `golden_regression_market_view_geometry` incorrectly describes the `between` representative as "B89: floor=89, cap=91 → InRange(89,91)" but the actual test fixture search is `26JUN13-B87.5` (floor=87, cap=88). The test logic is correct — the doc comment is stale. Not a logic defect.
  File: `/Users/xavierbriggs/fortuna/crates/fortuna-live/tests/aeolus_bucket_match.rs` (golden regression doc block lines 461–465 of diff)

- [Minor] The `kalshi_market_to_market_view` conversion leaves `category`, `resolution_source`, and `close_at` empty/None (explicitly commented). These are not needed for the weather-path bucket derivation, but callers that expect these fields on a MarketView from the weather source will receive empty values. The brief permits this and the comment is accurate; however, future callers (C2/C4) must be aware that `category` is empty on weather-path MarketViews. Should be ledgered in GAPS.md to warn C2/C4 implementers.
  File: `/Users/xavierbriggs/fortuna/crates/fortuna-venues/src/kalshi/weather.rs` lines 906–910

## Commands run (verbatim results)

```
grep -rn "kalshi::dto\|KalshiMarket\|KalshiMarketStatus" crates/fortuna-live/src/ ; echo "exit=$?"
→ exit=1   (no output; fully decoupled)

cargo fmt --all --check ; echo "fmt_exit=$?"
→ fmt_exit=0

git diff --name-only c4dec57..01c3f41 | grep fortuna-invariants ; echo "exit=$?"
→ (empty output) exit=1  (protected crate not touched)

SQLX_OFFLINE=true cargo clippy -p fortuna-cognition -p fortuna-venues -p fortuna-live --all-targets -- -D warnings
→ Finished `dev` profile [unoptimized + debuginfo] target(s) in 0.76s  (no warnings)

SQLX_OFFLINE=true cargo test -p fortuna-cognition -p fortuna-venues -p fortuna-live
→ All test result lines: ok. 0 failed (67+7+3+5+21+1+4+14+1+4+14 passed across binaries)
→ test golden_regression_market_view_geometry ... ok  (explicitly confirmed)

SQLX_OFFLINE=true bash scripts/run-dst.sh
→ [dst] OK: 15 corpus + 2000 random seeds, zero invariant violations
→ daemon_smoke: 29 passed, 0 failed; ingest-dst: 5 passed, 0 failed
→ All DST suites: 0 failures
```
