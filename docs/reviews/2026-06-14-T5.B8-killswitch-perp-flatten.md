# Review: T5.B8 — kill-switch PERP FLATTEN (spec 5.15) — 2026-06-14
Base: b4cd093  Head: 0d5b8f7  Verdict: ACCEPT
Protected crate touched: yes (ADD-ONLY — one new file, no existing assertion modified)

## Criteria (fixed before reading the diff, from spec 5.15 L308 + CLAUDE.md I1/I4/I5/I6 + T5.B8 contract)

- C1 I4 independence (spec L43, L308): PASS — `cargo tree -p fortuna-killswitch -e normal`
  shows the ONLY workspace deps are fortuna-core, fortuna-gates, fortuna-venues. Forbidden-set
  grep (sqlx|tokio-postgres|postgres|fortuna-ledger|fortuna-cognition) = zero hits. fortuna-gates
  itself = core+serde+thiserror only (no forbidden, no tokio). Existing
  `i4_killswitch_independence` test GREEN (1 passed, 14.25s) and UNTOUCHED by the diff
  (`git show 0d5b8f7 -- .../i4_killswitch_independence.rs` empty).

- C2 I1 the seal (spec 2.1, L123, 5.15 L296): PASS — `GatedPerpOrder` (crates/fortuna-gates/
  src/perp.rs:118) has private fields, `pub(crate) fn assemble` as THE ONLY constructor
  (:135), derives Serialize only (no Deserialize). fortuna-gates SOURCE untouched by the
  commit (empty diff). grep of all `GatedPerpOrder` refs outside fortuna-gates: only type
  usages / accessors / test helpers that obtain it via `evaluate_perp` — no struct literal,
  no From/Into, no Deserialize. The killswitch obtains every close via
  `gates.evaluate_perp(...).gated` (lib.rs:619); `GatedPerpOrder::assemble` is reached only
  after the full PERP_ALL trail passes (perp.rs:361-363).

- C3 No protected-crate / test weakening (CLAUDE.md): PASS — invariants change is exactly
  `A crates/fortuna-invariants/tests/perp_i4_flatten_seal.rs` (add-only). Whole-diff grep for
  deleted `assert`/`debug_assert`: NONE. No `#[ignore]`, no proptest case reduction, no
  loosened tolerance anywhere. The only `-` lines in code are an import line + the usage
  string in main.rs.

- C4 No panic / fail-closed (CLAUDE.md money-path rule): PASS — `#![cfg_attr(not(test),
  deny(unwrap_used,expect_used,panic,todo,unimplemented))]` (lib.rs:19) + `#![deny(...)]`
  (main.rs:13). grep of non-test killswitch src for unwrap()/expect(/panic!/todo!/
  unreachable!: NONE (line 458 is `.unwrap_or(...)`, the infallible-fallback idiom). No
  SystemTime/Instant/Utc::now (all time via injected Clock). Per-position no-price/gate-
  reject/place-error is journaled and the sweep CONTINUES (lib.rs:503-665); only a journal-
  write failure (`?`) aborts. `load_gate_config` + `load_kinetics_creds` refuse (exit 4)
  BEFORE any venue/signer/transport is built (main.rs:228-282), naming the env var never the
  value. clippy -D warnings clean.

- C5 Reduce-only correctness (spec 5.15 L300/L308): PASS — close action is opposite the
  position (long→Sell, short→Buy; lib.rs:517-528), qty = |pos| exactly (:562,:603).
  `conservative_mark = touch` (:612) and `limit = touch ∓ ceil(touch·bps/10_000)`
  (crossed_close_limit :328-339), so |limit−mark| = slippage; the ceil rounds slippage UP =
  limit further AGAINST us. A slippage > price_band self-rejects PriceSanity (gates perp.rs
  :796-802; reduce-only does NOT waive PriceSanity) — counted as `flatten_orders_rejected_
  by_gate`, never a wrong fill (flatten.rs test asserts no POST/margin/orders on reject). The
  gate's build_profile classifies valid reduce-only (opposite dir, qty<=|pos|) as
  reducing=true (perp.rs:560-574), waiving only MarginHeadroom/Liq/Leverage/Notional/Edge;
  same-dir/oversized/no-position → Err routed to MarginHeadroom reject (never seal). Balance-
  only MarginAccountView: equity is never read on the reduce-only path (no check that runs
  reads account.equity), so no fabricated marks/positions. Negative/overflow limit ⇒ skip or
  PriceSanity reject (fail-closed).

- C6 No secrets (CLAUDE.md): PASS — `KalshiCreds` has a hand-written Debug that redacts
  private_key_pem (lib.rs:205-213). PEM lives only in creds and is passed to KalshiSigner,
  never printed. No eprintln/println interpolates pem/private/secret/key_id. Diff grep for
  hardcoded KEY/TOKEN/SECRET string literals: NONE. The verb names env vars in usage/errors,
  never their values.

- C7 Tests prove the claims, non-vacuous: PASS — perp_i4_flatten_seal (5/5): (a) valid
  reduce-only seals with full PERP_ALL all-Pass trail + reduce_only()==true + correct
  action/qty at ZERO equity; (b/c/d) same-dir/oversized/no-position reject at MarginHeadroom
  and never seal; (e) walks the resolved `cargo metadata` NORMAL-dep graph asserting forbidden
  absent + fortuna-gates present. flatten.rs (8/8): long→SELL side=ask limit strictly below
  bid touch; short→BUY side=bid limit strictly above ask touch; reduce_only=true + IOC on the
  wire body; empty-book/orderbook-error skip; place-error counted+continue; cancel-all sweeps
  3 (NotFound + retry); gate-reject counted-not-fatal + never reaches the wire; load_gate_
  config fail-closed (none/blank/missing/unparseable/validate-fail/ok). All assertions read
  real report counters and real transport wire bodies.

## Findings
- [Minor / operational, NOT a defect] A non-flat position whose size exceeds the venue/strategy
  per-order cap (perp.venues.max_order_contracts or per_strategy max_order_notional_cents) will
  be SizeSanity-rejected by the gate (perp.rs:375-388) → counted as flatten_orders_rejected_
  by_gate, position left open, exit 5. This is correct fail-closed behavior consistent with the
  spec's "best-effort flatten, an accepted emergency cost" (L165) and the gate's I1 role — a
  single IOC larger than the venue max would be venue-rejected anyway. No reproduction of harm;
  reported as an operational characteristic the operator should know (a very large accumulated
  perp position cannot be flattened in one IOC by the switch). Not a BLOCK. Already implied by
  the "counted, never fatal; re-running is idempotent" design; consider an explicit GAPS note if
  multi-clip flatten is ever wanted.

No Critical, no Major findings. No invariant breach, no fail-open path, no GatedOrder bypass,
no secret, no nondeterminism, no test weakening.

## Commands run (verbatim results)
- cargo fmt --check -p fortuna-killswitch → exit 0
- cargo clippy -p fortuna-killswitch -p fortuna-gates -p fortuna-invariants --all-targets -- -D warnings
  → exit 0, 0 warning/error lines
- cargo test -p fortuna-invariants --test perp_i4_flatten_seal → 5 passed; 0 failed
- cargo test -p fortuna-invariants --test i4_killswitch_independence → 1 passed; 0 failed (EXISTING, untouched)
- cargo test -p fortuna-killswitch → flatten 8 passed; kalshi_freeze 1; kalshi_live_wiring 9; all 0 failed
- cargo tree -p fortuna-killswitch -e normal → deps = fortuna-core, fortuna-gates, fortuna-venues only;
  forbidden-set grep = 0 hits
- scripts/run-dst.sh 25 → DST-EXIT 0; all arms (perp-dst, funding-forecast-dst, perp-event-basis-dst,
  settlement, synthesis, persona) 0-failed, zero violations (full 200 run was executed by the main loop
  per the contract; representative run confirms no regression from this commit)
- git show 0d5b8f7 -- crates/fortuna-invariants/tests/i4_killswitch_independence.rs → empty (untouched)
- git show 0d5b8f7 --stat -- crates/fortuna-gates/ → empty (consume-only, source untouched)
