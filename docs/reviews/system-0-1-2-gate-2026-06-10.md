# Review: system-0-1-2-gate — 2026-06-10
Base: d138a73 (operator baseline)  Head: 1dcd05d (Phase 2 EXIT)  Verdict: BLOCK
Protected crate touched: yes (history audit below: additions/stub-implementations only; zero assertion-logic modifications)

Cumulative system gate over Phases 0+1+2. Reviewed in a detached scratch worktree
(/tmp/fortuna-sys012 at 1dcd05d); operator tree untouched. All evidence executed in
this session; DST run on a FRESH pinned master seed (20260610), not the implementer's.

## Criteria (fixed before reading any code)

### Phase 0 EXIT (BUILD_PLAN.md:70-72)
- A1 mech_structural full loop in Sim with replay: PASS — sim_loop.rs
  `mech_structural_captures_a_bracket_arb_end_to_end` +
  `same_seed_same_script_byte_identical_recording` green (workspace run, exit 0);
  `scripts/replay.sh --seed 1234567` -> "[dst] seed 1234567: OK".
- A2 DST corpus zero violations >= 10,000 seeds: PASS — fresh run
  `DST_MASTER_SEED=20260610 dst --seeds 10000` -> "[dst] OK: 0 corpus + 10000 random
  seeds, zero invariant violations".
- A3 CI battery green: PASS — `cargo fmt --check` clean; `cargo clippy --workspace
  --all-targets -- -D warnings` exit 0; `cargo test --workspace` CARGO_EXIT=0,
  580 passed / 0 failed / 2 ignored (the two T3-owned I7 stubs); .github/workflows/ci.yml
  present (hosted CI runs not verifiable from this session).
- A4 I1-I4 invariant tests implemented and green: PASS — per-test: i1_universal_gate ok,
  i1_prop_all_orders_carry_gate_verdicts ok, 2 compile-fail doctests ok,
  i2_drawdown_human_rearm ok, i2_prop_breach_always_locks_until_rearm ok,
  i3_runaway_halt ok, i4_killswitch_independence ok (12.27s, spawns the standalone binary).

### Phase 1 EXIT (BUILD_PLAN.md:121-122)
- B1 both mechanical strategies in Paper against recorded data streams: FAIL
  (OPERATOR-BLOCKED) — paper engine + parity proven at the gated-order boundary
  (paper.rs 10 tests incl. `touch_prints_never_fill_resting_orders`); recorded streams
  blocked on Kalshi fixture capture. GAPS.md:94-104: "OPERATOR-BLOCKED beyond it: the
  fixture capture must include websocket orderbook_snapshot/orderbook_delta and public
  trade messages."
- B2 settlement AND discrepancy paths exercised in DST: FAIL — settlement paths ARE in
  the seeded harness (dst.rs:435-496 settle|void|reverse arms, I-money through refunds);
  discrepancy/watchdog paths are NOT: `grep -iE 'discrepan|mismatch|overdue|dispute|watchdog'
  crates/fortuna-core/tests/dst.rs crates/fortuna-runner/tests/synthesis_dst.rs` -> zero
  scenario hits. Coverage exists only as deterministic composed tests
  (settlement_loop.rs: overdue_settlement_alerts_once,
  persistent_position_mismatch_writes_discrepancy_and_halts,
  disputed_market_freezes_the_position), which scripts/run-dst.sh never runs. The prior
  Phase 1 gate finding is STILL TRUE at 1dcd05d.
- B3 dashboards and digest render from sim data: PASS —
  observability.rs::dashboards_and_digest_render_from_sim_data green; fortuna-ops suites
  (config/dashboard/deadman/digest_export/metrics/slack) green in workspace run.

### Phase 2 EXIT (BUILD_PLAN.md:193-195)
- C1 full decision loop in Sim with StubMind under DST incl. cognition failures: PASS —
  synthesis_loop.rs (belief->comparator->proposal->fill; cognition-failure degrade;
  shadow no-trade) green; synthesis_dst at 10,000 scenarios: "totals: 33319 orders,
  51437 proposals, 132896 cognition failures, 117527 beliefs ... ok. 1 passed (86.51s)",
  byte-identical replay per seed.
- C2 AnthropicMind exercised behind feature flag with budget controls: FAIL
  (OPERATOR-BLOCKED) — built + mock-tested (CostBudget check-before-call, UTC day roll;
  mind.rs:279 env-key gate; tests/mind.rs::budgets_check_before_calling_and_roll_at_utc_midnight
  green). GAPS.md:20-24: "needs ANTHROPIC_API_KEY — the env-key gate IS the flag".
- C3 aeolus_eval writes scored beliefs from fixture envelopes: PASS —
  fixtures/aeolus/sample_envelope.json ->
  ledger.rs::aeolus_eval_writes_scored_beliefs_from_the_fixture_envelope green (Pg-backed).
  Operator-recorded real export remains open in GAPS (validates Aeolus's exporter, not this).
- C4 I5-I7 invariant tests green: FAIL (strict letter) — I5 green, I6 green (3 tests),
  i7_promotion_gates green; BUT 2 of 3 I7 clauses are #[ignore] todo!() stubs
  (i7_promotion_gates.rs:192,202) owned by T3.1/T3.3, whose rails are Phase 3 BUILD_PLAN
  tasks. Rubric-internal tension (exit line demands what BUILD_PLAN schedules later);
  ledgered in GAPS "Open". Operator adjudication requested.

### Invariants I1-I7 (CLAUDE.md Section 3)
- D1 I1 universal gate: PASS — tests green + structural: GatedOrder sealed (private
  fields, pub(crate) assemble sole ctor, Serialize-only, 2 compile-fail doctests);
  zero constructors outside fortuna-gates (all test helpers route a REAL GatePipeline);
  Venue::place takes only GatedOrder; SimVenue::place_raw is sim-internal scaffolding
  unreachable through the Venue trait (callers: sim's own place() + sim_venue.rs tests).
- D2 I2 drawdown halts, human re-arm: PASS — i2 tests green;
  ledger.rs::halts_fold_restores_only_unrearmed_flags (halts survive restart).
- D3 I3 runaway dual token-buckets: PASS — i3 green (halt survives time, re-arm only);
  per-venue AND per-market buckets in config ([rate.*] burst/sustained_per_min/
  market_burst/market_sustained_per_min).
- D4 I4 kill-switch out-of-band: PASS — i4 green (spawns binary with no DB,
  freeze-and-cancel against sim venue, dep-graph asserted via cargo metadata);
  independent check: `cargo tree -p fortuna-killswitch` has zero matches for
  sqlx|postgres|fortuna-ledger|fortuna-cognition; scripts/killswitch-test.sh exists.
- D5 I5 append-only audit: PASS — i5 green (mutation refused at DB, dead-store halt
  contract); ledger tests audit_rows_refuse_mutation_at_the_database,
  intent_events_refuse_mutation; sim_loop.rs::audit_sink_failure_halts_trading.
- D6 I6 propose-only Mind: PASS — i6 x3 green (smuggled sizing fields reject whole
  output; no executable side effects; mind crate cannot name a venue).
- D7 I7 promotion gates: PASS for all rails that exist — stage ladder ordered, SimRunner
  refuses any strategy staged above Sim at construction, no programmatic promotion path
  exists. The two future-rail clauses are staged stubs (see C4 FAIL).

### PROMPT.md acceptance items applicable to Phases 0-2
- E1 BUILD_PLAN tasks complete with evidence notes (T0.1-T2.8): PASS — all 23 ticked
  with notes + commit hashes.
- E2 fmt/clippy/full suite green: PASS — fmt clean; clippy -D warnings exit 0;
  CARGO_EXIT=0, 580/0 failed/2 ignored.
- E3 invariant tests implemented, none ignored: FAIL by letter — 2 ignored (the T3.1/T3.3
  I7 stubs). Final closure owned by T3.x/T3.6 per GAPS; repo-wide there are no other
  #[ignore] anywhere.
- E4 DST corpus: doctrine scenarios present; 10,000+ seeds; regression seeds committed:
  FAIL — (a) seeds: PASS, 10,000 core + 10,000 synthesis fresh seeds, zero violations;
  (b) scenario inventory: in-harness = crash-pre-submit, crash-pre-ack, dup fill, fill
  after cancel, partial-fill+outage, settle-then-reverse, reservation rebuild at boot,
  schema-invalid output, budget exhaustion (9-10/20); sanctioned-isolation = kill switch
  (i4); TEST-ONLY (outside scripts/run-dst.sh) = overdue watchdog, halt-mid-IntentGroup,
  rate-limit mid-burst, malformed payloads, clock skew, stale-mark drawdown, trigger
  storm, max-leg-duration (8/20); PARTIAL = Pg-down (mid-run halt contract proven;
  at-boot not demonstrated); (c) regression corpus EMPTY: dst-corpus/ holds README.md
  only, "[dst] regression corpus: 0 seed(s)", while the T0.4 note claims red runs
  occurred ("2 real harness bugs found+fixed on first runs").
- E5 replay determinism, same seed byte-identical: PASS — bus replay suite (regen,
  divergence, tamper, truncation), DST I-determinism (every scenario runs twice),
  sim_loop/veto_loop byte-identical recordings, synthesis_dst replay invariant,
  replay.sh demo.
- E6 mech_structural + mech_extremes end-to-end Sim AND Paper-with-recorded-data: FAIL
  (OPERATOR-BLOCKED on fixtures; Sim end-to-end PASS for both; Paper realism doctrine
  test green).
- E7 belief pipeline end-to-end in Sim + aeolus_eval vs sample fixture: PASS (C1+C3).
- E8 kill-switch standalone (no Pg, runtime dead, freeze-and-cancel, monthly script):
  PASS — i4 + scripts/killswitch-test.sh.
- E9 Slack routing, digest, dead-man, accounting export, dashboard vs sim data: PASS —
  fortuna-ops suites + observability end-to-end test green.
- E10 GAPS.md operator-blocked-only: FAIL — "Open" section holds non-operator-blocked
  items, two of them STALE: divergence detector "deferred to T2.1" (GAPS.md:57-62) but
  T2.1 closed without it; belief-staleness watchdog "deferred to T2.3" (GAPS.md:63-66)
  but T2.3 closed without wiring it. Neither has a current owner.
- OUT-OF-SCOPE (Phase 3+): FINAL_REPORT.md, full acceptance-checklist run (T3.6),
  Polymarket adapter (T3.4), synth_events (T3.5), weekly/monthly review jobs (T3.1),
  shadow comparison harness (T3.3).

### Repo-wide mechanical sweep (at 1dcd05d)
- F1 unwrap/expect/panic/todo in money paths: PASS — only 2 hits, both inside
  #[cfg(test)] modules (gates/rate.rs:62, kalshi/adapter.rs:1098).
- F2 clock discipline: PASS — zero SystemTime::now/Instant::now/Utc::now/Local::now
  outside clock impls and tests.
- F3 f64 money leakage: FAIL (Minor) — fortuna-state/src/sizing.rs:68 computes the cents
  budget as `(headroom.raw() as f64 * f).floor()` (CLAUDE.md: "Never f64 for money...
  in the core"; arithmetic at conversion boundaries belongs to rust_decimal). Direction
  is conservative (floor) and exact below 2^53 cents; NOT ledgered in ASSUMPTIONS.md.
  All other hits are probabilities or Decimal-converting DTO boundaries (NaN/Inf rejected).
- F4 unordered-map nondeterminism: PASS — zero HashMap/HashSet in
  core/gates/exec/state/runner sources; BTree throughout; deterministic-serialization
  test pins map-key ordering.
- F5 secrets: PASS — pattern sweep over code/config/fixtures clean; config/fortuna.toml
  carries no credentials; Anthropic key is env-only and never logged.
- F6 GatedOrder bypass: PASS — see D1.
- F7 test weakening (history-wide): PASS — `git log -p` over all test files: zero deleted
  assert/prop_assert lines; no proptest case reductions; no #[ignore] outside the two
  staged I7 stubs.

### Protected-crate commit audit (crates/fortuna-invariants/, full history)
- d138a73 baseline: operator-authored stubs (7 invariant files, each #[ignore]+todo!()). 
- f463de2 (T0.5): implements I1+I3 stubs; deletions are ONLY the placeholder
  `#[ignore...]`/`todo!()` lines (quoted in evidence). Pure stub-implementation.
- 11e8313 (T0.7): implements I2 stub; same pattern. Pure stub-implementation.
- a1e4449 (T0.8): implements I5 stub; same pattern. Pure stub-implementation.
- d5e2220 (T0.9): implements I4 stub; same pattern. Pure stub-implementation.
- 88c9b91 (T1.3): +3 lines, zero deletions. Pure addition.
- 1dcd05d (Phase 2 EXIT): implements I6 + I7 stubs; deletions are only placeholder
  lines; ADDS two new #[ignore] staged stubs (T3.1/T3.3) following the baseline's own
  pattern. Pure stub-implementation + additions. FLAGGED for operator adjudication:
  no assertion logic was modified or weakened anywhere in history.
- Suite at 1dcd05d: 11 runtime tests + 3 doctests green; 2 ignored (the staged stubs).

## Findings
- [Major] Discrepancy/watchdog paths (overdue, dispute, position-mismatch->global-halt)
  absent from BOTH seeded DST harnesses, contra Phase 1 EXIT "exercised in DST"; prior
  gate finding still true at 1dcd05d — reproduction: grep of dst.rs + synthesis_dst.rs
  (zero hits); coverage only in settlement_loop.rs which scripts/run-dst.sh never runs.
- [Major] Spec 5.13 divergence detector (venue outcome vs canonical criteria; edge-
  confidence hit) does not exist at Phase 2 exit; its GAPS entry still says "deferred to
  T2.1" though T2.1 closed without it — reproduction: grep -i divergence over
  cognition/runner/state src (only the unrelated PriceBeliefDivergence trigger rule);
  GAPS.md:57-62.
- [Major] Belief-staleness/stranded watchdog (open position + no fresh belief) is
  policy-only (beliefs.rs Freshness::Stale), never wired into run_watchdogs (covers
  confirm/overdue/dispute/mismatch only); GAPS entry stale ("deferred to T2.3") —
  reproduction: runner.rs:1374-1420 + grep; GAPS.md:63-66.
- [Minor] DST regression corpus is empty (0 .seed files) while T0.4's note claims red
  runs occurred; PROMPT "regression seeds committed" has no evidence — reproduction:
  ls dst-corpus/ -> README.md only; harness prints "regression corpus: 0 seed(s)".
- [Minor] scripts/run-dst.sh fails OPEN: if the dst target fails to BUILD, the script
  prints "passing vacuously" and exits 0 with stderr suppressed (lines 13-19) —
  verification infrastructure should fail closed.
- [Minor] f64 money arithmetic at kelly_contracts (sizing.rs:68), conservative direction
  but contra CLAUDE.md convention and unledgered in ASSUMPTIONS.md.
- [Minor] run_watchdogs early-returns when venue.positions() errors (runner.rs:1375-1378),
  starving the overdue/dispute checks that need no venue data — overdue alerts are
  deferred precisely during the outages that cause them.
- [Info] Two new #[ignore] staged I7 stubs added at 1dcd05d in the protected crate
  (additions, GAPS-ledgered, T3.1/T3.3 owners) — flagged per cumulative-review rule.
- [Info] OPERATOR-BLOCKED gate items with exact unblock steps in GAPS: B1/E6 (Kalshi
  websocket fixture capture), C2 (ANTHROPIC_API_KEY smoke call), aeolus real export.

## Verdict rationale
No Critical findings: no invariant violation, no money-path defect, no test weakening,
no nondeterminism, no secrets; 20,000 fresh seeds + 580 tests green. The gate verdict is
BLOCK because phase-gate grading requires every EXIT item PASS and six items fail:
three implementer-fixable Majors (DST discrepancy arms; divergence detector; staleness
watchdog — the last two hiding behind stale GAPS deferrals), corpus/ledger Minors, and
two operator-blocked items. Remediation is narrow and enumerated; nothing found
invalidates the built system's core soundness.

## Commands run (verbatim verdict lines)
- cargo fmt --check -> FMT_CLEAN (exit 0)
- SQLX_OFFLINE=true cargo clippy --workspace --all-targets -- -D warnings -> "Finished `dev` profile [unoptimized + debuginfo] target(s) in 18.78s" (exit 0)
- DATABASE_URL=postgres://localhost/fortuna_dev cargo test --workspace -> CARGO_EXIT=0; passed=580 failed=0 ignored=2 (80 result lines)
- DST_MASTER_SEED=20260610 dst --nocapture --seeds 10000 -> "[dst] OK: 0 corpus + 10000 random seeds, zero invariant violations" (exit 0)
- DST_MASTER_SEED=20260610 SYNTH_DST_SCENARIOS=10000 synthesis_dst --nocapture -> "[synthesis-dst] totals: 33319 orders, 51437 proposals, 132896 cognition failures, 117527 beliefs"; "test result: ok. 1 passed ... finished in 86.51s"
- cargo test -p fortuna-invariants -> 11 passed, 2 ignored (i7 T3.1/T3.3 stubs), 0 failed; doctests 3 passed (2 compile-fail)
- bash scripts/replay.sh --seed 1234567 -> "[dst] seed 1234567: OK"
- cargo tree -p fortuna-killswitch | grep -iE 'sqlx|postgres|fortuna-ledger|fortuna-cognition|anthropic' -> no matches (exit 1)
- git log -p (history-wide test files) | grep '^-...assert' -> zero deleted assertions
- psql postgres://localhost/fortuna_dev -c 'select 1' -> 1 row
