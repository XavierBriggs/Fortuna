# Review: phase-3-gate — 2026-06-10
Base: 1dcd05d  Head: 7bbc3ef  Verdict: BLOCK (automatic: protected crate touched; zero content findings — operator sign-off on the i7 stub-closure diff converts this to ACCEPT)
Protected crate touched: yes (crates/fortuna-invariants/tests/i7_promotion_gates.rs)

Rubric: BUILD_PLAN.md Phase 3 EXIT — "PROMPT.md acceptance checklist fully green or
operator-blocked-only" — graded per item against PROMPT.md lines 99-119 (11 bullets).
On the merits, the rubric is MET: every item is GREEN with executed evidence or
OPERATOR-BLOCKED with exact unblock instructions in GAPS.md. The BLOCK is the
CLAUDE.md/verifier hard rule that any diff touching crates/fortuna-invariants/ is an
automatic BLOCK pending operator review, regardless of content.

## Protected-crate diff, line by line (the only file: tests/i7_promotion_gates.rs)
DELETED (exactly four things, nothing else):
- `#[ignore = "implement per BUILD_PLAN (T3.1); see tests/README.md"]` + the
  `todo!("encode: a stage promotion takes effect only with an operator action record...")`
  body of `i7_stage_promotion_requires_operator_action_record`.
- `#[ignore = "implement per BUILD_PLAN (T3.3); see tests/README.md"]` + the
  `todo!("encode: a model swap into the live decision flow requires a shadow comparison
  record...")` body of `i7_model_swap_requires_shadow_comparison_record`.
ADDED: implemented assertion bodies for both tests (no-record->Sim, contiguous-chain,
no stage skipping, "system"/blank actor cannot promote, declared stage is a cap,
demotion applies any-actor; empty/insufficient/worse shadow record -> Hold, qualifying
record -> recommendation-only verdict). No pre-existing assertion was modified,
deleted, or loosened; `i7_promotion_gates` and all other tests in the file untouched.
This is the baseline's sanctioned #[ignore]+todo!() stub-closure pattern (stubs were
operator-acknowledged staged work per the phase-2 gate review). Both tests import
pre-existing dev-deps (fortuna-runner, fortuna-cognition already in Cargo.toml).

## Criteria (fixed before reading the diff; PROMPT.md lines 99-119)
- C1 All BUILD_PLAN tasks complete with evidence notes: GREEN — grep '^\- \[ \]' = 0
  unchecked, 29 [x] each with a DONE-<commit> evidence note (29 matches).
- C2 fmt/clippy/suite: GREEN — `cargo fmt --check` exit 0; `cargo clippy --workspace
  --all-targets -- -D warnings` finished clean; `cargo test --workspace` 615 passed,
  0 failed, 0 ignored, exit 0 (DATABASE_URL=postgres://localhost/fortuna_dev).
- C3 Invariant tests implemented, none ignored, green: GREEN — `cargo test -p
  fortuna-invariants`: 13 tests (i1:2, i2:2, i3:1, i4:1, i5:1, i6:3, i7:3), 0 ignored,
  all ok; no `#[ignore]`/`todo!()` remains anywhere in the crate.
- C4 DST corpus (doctrine scenarios; 10,000+ seeds; regression seeds committed): GREEN
  — ran `./scripts/run-dst.sh 10000` myself: stage 1 "[dst] OK: 0 corpus + 10000
  random seeds, zero invariant violations" (master seed 1781122041528, fresh, not the
  FINAL_REPORT's); stage 2 "[synthesis-dst] totals: 33509 orders, 51320 proposals,
  131846 cognition failures, 116352 beliefs", 1 passed in 88.34s (master seed
  1781122428504); dst exit 0. All 20 doctrine scenarios map to a covering arm/test
  (dst.rs crash/dup/cancel/outage arms; groups_flatten max_leg_open_ms; settlement_loop
  reversed/overdue/divergence; sim_loop.rs:271 "halt mid-IntentGroup (doctrine
  scenario)" and :305 "audit failure halts trading"; marks.rs skew/stale; mind.rs
  schema-invalid + budget; signals.rs debounce; killswitch self-test). Regression-seed
  clause is VACUOUSLY satisfied: 0 seeds committed because git history shows no .seed
  file was ever committed OR deleted (no red seed ever found); corpus discipline
  (README, replay-first script, never-delete) is in place and FINAL_REPORT discloses.
- C5 Replay determinism: GREEN — sim_loop.rs:263 same_seed_same_script_byte_identical_
  recording ok; settlement_processing_is_byte_deterministic ok; synthesis DST asserts
  byte-identical replay per seed across all 10,000 scenarios (ran above).
- C6 mech_structural + mech_extremes Sim and Paper: GREEN(Sim)/OPERATOR-BLOCKED(Paper
  vs recorded streams) — mech_structural_captures_a_bracket_arb_end_to_end ok,
  mech_extremes suite ok, paper_live_parity_through_the_order_manager ok. Recorded-
  stream replay blocked on Kalshi fixtures; GAPS.md unblock: "operator records demo-env
  fixtures per crates/fortuna-venues/tests/kalshi_doc_samples/README.md" with "the SAME
  capture must also include websocket orderbook_snapshot/orderbook_delta + public trade
  messages (paper-engine recorded-stream replay...)".
- C7 Belief pipeline end-to-end in Sim; aeolus_eval vs sample fixture: GREEN(StubMind +
  fixture)/OPERATOR-BLOCKED(AnthropicMind live) — synthesis_belief_trades_through_the_
  full_loop ok; aeolus_eval_writes_scored_beliefs_from_the_fixture_envelope ok against
  fixtures/aeolus/sample_envelope.json; ANTHROPIC_API_KEY verified UNSET in-session;
  GAPS.md unblock: "operator provisions .env per README... Recommended first exercise:
  one live smoke call against claude-haiku-4-5 under a tight CostBudget."
- C8 Kill-switch standalone + monthly script: GREEN — executed
  `./scripts/killswitch-test.sh` (env -u DATABASE_URL): "self-test OK: cancelled 2/2
  orders, reported 1 positions" -> "[killswitch-test] PASS"; i4_killswitch_independence
  ok (7.87s); script exists at scripts/killswitch-test.sh.
- C9 Slack/digest/dead-man/export/dashboard vs sim data: GREEN —
  dashboards_and_digest_render_from_sim_data, digest_is_deterministic_...,
  export_writes_once_and_refuses_overwrite, deadman tests, slack router tests (mock
  transport) all ok in suite. Socket-Mode interactivity listener OPERATOR-BLOCKED
  (Slack app credentials; GAPS.md quotes the research contract and allow-listing need).
- C10 GAPS.md operator-blocked-only with exact unblocks: GREEN — six entries (Kalshi
  fixtures, kill-switch venue plug, credentials, Aeolus export incl. the exact ssh
  command, Polymarket research+fixtures sequence, spec v0.9 touch-up), each with
  explicit unblock steps; "Disputed invariant tests: (none)".
- C11 FINAL_REPORT.md: GREEN — exists with what-was-built, 8 deviations with rationale,
  verification statistics (consistent with my independent runs: 615 tests, 13
  invariants, 10k+10k DST), per-item checklist disposition, operator go-live runbook
  (6 steps + operational pins).

## Findings
- [Critical-by-rule] Protected crate touched -> automatic BLOCK pending operator review
  (CLAUDE.md protected-directory rule) — reproduction: `git diff 1dcd05d...7bbc3ef --
  crates/fortuna-invariants/`. Content review found ZERO weakening (see line-by-line
  section); operator sign-off resolves.
- [Minor] "Regression seeds committed" is satisfied only vacuously (0 seeds; no .seed
  ever committed or deleted per `git log --all --diff-filter=D -- '...*.seed'`);
  cannot be verified from artifacts that no red seed was ever encountered off-record —
  FINAL_REPORT disclosure + intact never-delete discipline accepted as evidence.
- [Minor] fortuna-cognition/src/review.rs:278 casts money to f64 (`fees_cents as f64 /
  realized_pnl_cents as f64`) for the monthly cost-audit ratio — recommendation-only
  output, never feeds sizing/orders; div-by-zero yields inf which fails toward
  flagging. Convention note vs "no f64 money in the core"; ledger in GAPS not required
  (cognition, non-money-path) but worth an ASSUMPTIONS line.
- [Minor] Spec 5.13 says the divergence detector "writes a settlement_divergence on
  the market_event_edge"; implementation writes a discrepancy audit row carrying the
  edge id + documented confidence-hit directive, with the superseding edge insert
  delegated to the composition (runner.rs check_settlement_divergence doc). Behavior
  ("recorded, never reconciled away"; PnL follows venue truth) is honored and pinned by
  settlement_divergence_records_and_pnl_follows_venue_truth.
- [Info] Orphan watchdog is silent unless the composition wires the coverage view
  (position_coverage: None) — documented in code and tested both wired/unwired; live
  composition is itself operator-blocked.
- [Info] Four tracked .DS_Store files (repo root, docs/, docs/research/, docs/research/
  venue/) — pre-date this phase range (not in the diff); housekeeping for the operator.

## Commands run (verbatim verdict lines; all in detached worktree /tmp/fortuna-gate-p3 @ 7bbc3ef)
$ cargo fmt --check                                        -> exit 0
$ cargo clippy --workspace --all-targets -- -D warnings    -> Finished `dev` profile [unoptimized + debuginfo] target(s) in 19.38s   (clean)
$ DATABASE_URL=postgres://localhost/fortuna_dev cargo test --workspace
  -> passed:615 failed:0 ignored:0 ; exit 0
$ DATABASE_URL=... ./scripts/run-dst.sh 10000
  -> [dst] regression corpus: 0 seed(s)
  -> [dst] master seed 1781122041528 -> 10000 random scenario(s)
  -> [dst] OK: 0 corpus + 10000 random seeds, zero invariant violations
  -> [synthesis-dst] master seed 1781122428504 -> 10000 scenario(s)
  -> [synthesis-dst] totals: 33509 orders, 51320 proposals, 131846 cognition failures, 116352 beliefs
  -> test result: ok. 1 passed; 0 failed; 0 ignored; ... finished in 88.34s ; dst exit: 0
$ DATABASE_URL=... cargo test -p fortuna-invariants
  -> 13 tests across i1-i7 all ok; every block "0 failed; 0 ignored"
$ ./scripts/killswitch-test.sh /tmp/p3-killswitch-journal.jsonl   (DATABASE_URL deliberately unset by the script)
  -> self-test OK: cancelled 2/2 orders, reported 1 positions
  -> [killswitch-test] PASS — record this run in the ops log / audit.
$ [ -n "$ANTHROPIC_API_KEY" ]                              -> unset
$ git log --all --oneline --diff-filter=D -- 'crates/fortuna-core/dst-corpus/*.seed' -> empty (no seed ever deleted)
$ mechanical sweeps over git diff 1dcd05d...7bbc3ef: unwrap/expect/panic/todo in new
  src: none (all hits in tests); SystemTime/Instant/Utc::now: none in code; new
  #[ignore]: none in code; deleted asserts/proptest reductions/loosened tolerances:
  none; secret literals: none; GatedOrder construction: only via GatePipeline::evaluate
  (test helper gate_leg) — no bypass.
