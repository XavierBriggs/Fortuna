# Review: T5.B8 (perp basis-v2 A10 diagnostics → MetricSamples) — 2026-06-14
Base: track-c HEAD (9466a08, uncommitted working tree)  Head: working tree  Verdict: ACCEPT
Protected crate touched: no

## Criteria (fixed before reading the diff)
- C1 Additive / no regression (CLAUDE.md "Definition of done" + task req 1): PASS — `git diff` deletion count = 0; all 4 hunks are pure insertions (`@@ -N,0 +M,K @@`). Only 4 files changed (lib.rs, perp_event_basis_v2.rs, runner.rs, tests/perp_event_basis_v2.rs); none in crates/fortuna-invariants/. runner.rs change is a 9-line append at END of metrics_export (line 2553) after the last existing samples.push and before `samples` return — existing samples + order preserved. New trait method has default `Vec::new()`; the 5 other `impl Strategy` blocks (mech_extremes, mech_structural, synthesis, perp_event_basis rung-0, funding_forecast) are untouched and compile (clippy --all-targets green).
- C2 Discipline: no panic/unwrap/expect; finite-guarded casts; static-str names; no money misuse (CLAUDE.md house style): PASS — grep of added source lines: zero `panic!`/`unwrap(`/`expect(`/`todo!`/`unimplemented!`. Every f64→i64 cast finite-guarded (cdf: Some+is_finite @1432-1433; sigma_tau: is_finite @1446; anchor: is_finite @1458; tau_ms: i64 Some-guard @1469; obs_count: usize; anchor_stale: i64::from(bool)). All name/help are `&'static str` literals; per-market value in `market` label. Only `Cents` mention is a doc-comment "NOT money — no Cents". No SystemTime/Instant/Utc::now. Scalings documented in code comments AND metric help strings.
- C3 Correctness: values match V2Eval×scale; market label on every sample; not-ready→active=0; tests non-vacuous (task req 3): PASS — override (perp_event_basis_v2.rs 1399-1515) read against V2Eval struct (438-489): each value = its field × documented scale. not-ready path returns exactly one active=0 sample with market label. The 4 tests assert against a REAL priced eval (priced_v2_strategy reuses the hand-checked straddle/coherent-book fixture; preconditions assert cdf_divergence.is_some() + !q_j.is_empty()); scaling-pin test computes want=(field*scale).round() as i64 from eval's own f64 (catches mis-scale to x100/x1000); regime=direct asserted at τ=2h ≤ direct_max=4h; default-trait test uses a dummy non-overriding Strategy.
- C4 Invariants I1-I7 structurally untouched (telemetry path): PASS — diff touches no order/gate/exec/money/ledger code (telemetry integers only). fortuna-invariants crate run green (all suites 0 failed, incl. perp I1/I2/I3 + GatedOrder compile-fail doc-tests); no i5 sqlx duplicate-key race observed.
- C5 Full battery passes (CLAUDE.md DoD 2; task req 5): PASS — see Commands.

## Findings
(none — no Critical / Major / Minor)

## Commands run (verbatim results)
- `cargo fmt --check` → exit 0 (no output).
- `SQLX_OFFLINE=true cargo clippy --workspace --all-targets -- -D warnings` → exit 0; zero warning/error lines; `Finished dev profile`.
- `cargo test -p fortuna-runner --test perp_event_basis_v2` (DATABASE_URL=postgres:///fortuna?host=/tmp, SQLX_OFFLINE) → `test result: ok. 47 passed; 0 failed; 0 ignored` — incl. metric_samples_priced_headline_set, metric_samples_priced_scalings_pinned, metric_samples_not_ready_emits_only_active_zero, metric_samples_default_trait_method_is_empty.
- `cargo test -p fortuna-invariants` (test DB, offline) → all suites ok, exit 0; e.g. perp_i2 3/3, perp_i3 3/3, perp_i1 1/1, lib 6/6, 6 compile-fail doc-tests ok.
- `cargo test --workspace` (test DB, offline) → exit 0 (process exit authoritative; cargo --workspace returns non-zero on any failure); captured tail shows only `test result: ok ... 0 failed`.
- `SQLX_OFFLINE=true DATABASE_URL=postgres:///fortuna?host=/tmp scripts/run-dst.sh` → exit 0; `[dst] OK: 5 corpus + 2000 random seeds, zero invariant violations`; all 10 DST suites (dst, synthesis_dst, settlement_dst[2000], perp_dst[2000], funding_forecast_dst, perp_event_basis_dst, persona_dst, persona_orchestrator_dst, daemon_smoke, ingest_dst) reported 0 failed; no violation/panic/red-seed signature anywhere.
