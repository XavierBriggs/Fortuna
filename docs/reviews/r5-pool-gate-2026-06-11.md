# Review: T4.3 ROTA slice 5 (R5 pool) + carried minors — 2026-06-11
Base: 7e35f51  Head: 468c8c1  Verdict: ACCEPT-WITH-GAPS
Protected crate touched: no (0 files under crates/fortuna-invariants/ in range)

Reviewed at COMMIT 468c8c1 in a detached worktree (/tmp/fortuna-g8, removed after);
the dirty main tree was never graded. Range = c953af4 (scheduler/digest honesty),
fd3645b (verifier-session re-gate record, docs-only), 9d351b5 (dead-man ledger
reconciliation, docs-only), 468c8c1 (R5 pool + money design-block).

## Criteria (fixed before reading the diff: GATE-FINDINGS items 3-4 + ROTA
## AMENDMENTS R5/R3/R6/R8 + CLAUDE.md battery)

- A3 DailyScheduler/digest honesty (carried minor 3): PASS —
  (a) restart-fire ledgered as deliberate in GAPS "#3(a) fire-on-boot: LEDGERED
  as INTENDED" with rationale; `due()` unchanged and
  `daily_scheduler_fires_once_per_utc_day ... ok` (run this session).
  (b) label now "FORTUNA digest <date> (sim, cumulative since boot)"
  (daemon.rs terse_daily_digest); test
  `terse_daily_digest_labels_its_counters_honestly_as_since_boot ... ok` (run).
  (c) drive()-level assertion committed in daemon_smoke (COUNT(*) of kind='alert'
  payload LIKE 'FORTUNA digest%' == 1);
  `daemon_smoke_boot_ticks_signal_shutdown ... ok` (run). Spec basis verified:
  spec.md:349 "every Slack message is also an audit row".
- A4 GAPS/ASSUMPTIONS dead-man contradiction (carried minor 4): PASS —
  ASSUMPTIONS.md entry rewritten as a DESIGN NOTE (RealClock is the sanctioned
  wall source; "NO ledger exception is needed (consistent with the GAPS note)");
  GAPS.md:141-148 gained the matching reconcile clause. Code claim re-verified:
  `grep -rn "SystemTime::now" crates/fortuna-live/src/` = 1 hit, a COMMENT at
  main.rs:59; wall reads via RealClock.now() at main.rs:61 and main.rs:155.
- B1 dedicated pool, distinct + bounded (R5): PASS — two construction sites:
  main.rs:64 `fortuna_ledger::connect(...)` (writer; runs MIGRATOR; feeds
  compose_runner:68 and PgHaltPoller:168) vs main.rs:93
  `fortuna_ledger::connect_readonly_pool(...)` feeding ONLY RotaState.pool
  (main.rs:108). Separate `PgPoolOptions::new().connect(...)` — not a clone.
  Values (fortuna-ledger/src/lib.rs:79-96): max_connections(2),
  acquire_timeout(3s), `SET statement_timeout = 3000` via after_connect on every
  connection (covers the audit-tail query — the only Pg view query; snapshot
  views never touch Pg). Distinctness proven EMPIRICALLY (scratch test, below):
  pg_sleep(10) on the ROTA pool cut at ~3s by statement_timeout while
  pg_sleep(4) on the writer pool ran to completion; writer INSERT into audit
  completed <900ms while both ROTA connections were held.
- B2 timeout/exhaustion => degraded 200, isolation from the audit writer:
  BEHAVIOR VERIFIED / COMMITTED COVERAGE UNVERIFIED-BY-TEST — no committed test
  saturates the pool. Verifier-executed scratch reproductions (worktree-only,
  deleted after): (1) fortuna-live scratch vs the REAL connect_readonly_pool:
  both connections held -> audit_tail_page errors bounded (~3s, never hangs);
  concurrent writer-pool INSERT proceeds <900ms; statement_timeout cuts
  pg_sleep(10) at ~3s — `test real_rota_pool_is_isolated_bounded_and_
  statement_capped ... ok` (10.74s). (2) fortuna-ops scratch vs the handler over
  HTTP with a saturated faithful-replica pool: GET /api/rota/v1/audit -> HTTP
  200, rows=[], error string present, bounded — `test exhausted_pool_renders_
  the_audit_panel_degraded_200 ... ok` (7.37s). Finding 1 (Minor) records the
  missing committed test.
- B3 audit tail end-to-end through the real pool: PASS —
  `audit_handler_serves_the_live_tail_when_a_pool_is_present ... ok` (handler
  layer, HTTP, available:true, cursorless => newest row present, next_after =
  newest id); `audit_tail_cursorless_returns_the_latest_page_not_the_oldest ...
  ok` (also covers cursor-forward and past-end-empty — the GAPS "cursor-
  pagination test DONE" claim is accurate);
  `audit_tail_empty_table_is_an_empty_page_not_an_error ... ok`; pool-absent
  degraded: `degraded_surfaces_are_200_with_explicit_unavailable ... ok`
  (asserts /audit available:false + empty rows at 200). Pg-DOWN with a LIVE
  pool has no committed test (rolled into Finding 1); the handler Err arm's
  200-never-500 was proven by scratch execution.
- B4 money design-block real, specific, ledgered: PASS — GAPS "MONEY VIEW —
  DESIGN-BLOCKED" names the exact mismatch; verified against code:
  `SimVenue::inspect_totals()` at fortuna-venues/src/sim.rs:289 returns
  `(Cents, Cents, usize, usize)` = (cash, reserved, fill_count, pending_count),
  NOT settled/committed/floating; positions are per-market, not
  strategy-attributed. No half-built money view: fortuna-live/src/views.rs
  mentions money only in deferred comments; /api/rota/v1/money renders
  status:"unavailable" (test green). The §5 identity (total = settled +
  floating, R6) genuinely has no faithful source today.
- B5 lock rule + no new mutating routes: PASS — fortuna-ops/src untouched in
  range (no route changes); route-table test `every_path_is_get_only_and_200
  ... ok` (7 paths x POST/PUT/DELETE/PATCH -> 405, includes /api/rota/v1/audit);
  `favicon_is_a_204_not_a_404 ... ok`. R8 by reading: audit_tail never takes
  the snapshot lock; read_view clones inside a block and releases before
  returning (rota.rs:72-85); view_streams clones generated_at in a block
  (rota.rs:173) before the filesystem scan.
- C battery: PASS — verbatim results below. Tier: DST at 2000 (task-review
  default for a read-path-only range — dashboard wiring, daemon main, ledgers;
  zero money-path changes; the 10000 bar stays for money-path/phase gates).
- C mechanical sweep: PASS — new `unwrap(` only in test files; zero
  unwrap/expect/panic/SystemTime::now/Instant::now/Utc::now/f64/f32 in src
  additions; no HashMap iteration feeding bus/audit/sizing; no `place(` sites;
  no secret literals (DB URL via the secret wrapper's .expose(), never logged).
- C test-weakening sweep: PASS — diff-wide grep for removed
  assert/#[ignore]/proptest-cases/tolerances: the single "-...assert" hit is a
  GAPS.md PROSE line; no test deletions, no #[ignore], no case-count or
  tolerance changes.

## Findings

- [Minor] R5's saturation/isolation property has NO COMMITTED test (pool
  exhaustion -> degraded 200; concurrent daemon-pool audit append unimpeded;
  statement_timeout bound). Behavior verified correct by the two verifier
  scratch reproductions quoted above — reproduction recipe: hold both
  connections via pool.acquire() x2, GET /api/rota/v1/audit (expect 200 +
  error field in ~3s), INSERT INTO audit via the writer pool concurrently
  (expect <1s), `SELECT pg_sleep(10)` on the ROTA pool (expect statement-
  timeout error at ~3s). Recommend committing an equivalent test (the
  fortuna-ops handler-level version needs only the existing dev-deps via
  `#[sqlx::test]`'s PoolOptions/ConnectOptions injection). Ledger in GAPS.
- [Informational] audit_tail's Err arm (rota.rs:271-276) returns
  `"available": true` with raw `e.to_string()` (sqlx/Pg error text) to the
  client. No secret observed in those messages; surface is localhost,
  read-only. Consider available:false + neutral detail when the read fails.
- [Informational] crates/fortuna-core/dst-corpus/ holds only README.md (the
  "[dst] 0 corpus" line) — no regression seed has ever been recorded.
  Pre-existing (T0.4), consistent with no red seed ever found; not a range
  defect. Flagged so the empty corpus is a known fact, not an assumption.

## Commands run (verbatim results, all at 468c8c1 in /tmp/fortuna-g8,
## DATABASE_URL=postgres://localhost/fortuna_dev)

- cargo fmt --check                                  -> FMT_EXIT=0
- cargo clippy --workspace --all-targets -- -D warnings -> CLIPPY_EXIT=0
  ("Finished `dev` profile ... in 28.82s")
- cargo test --workspace                             -> WORKSPACE_EXIT=0;
  TOTAL passed=720 failed=0 ignored=0 (107 "test result:" lines)
- cargo test -p fortuna-invariants (per-test)        -> 13 tests + 3 doc-tests,
  every line "... ok": i1_universal_gate, i1_prop_all_orders_carry_gate_verdicts,
  i2_drawdown_human_rearm, i2_prop_breach_always_locks_until_rearm,
  i3_runaway_halt, i4_killswitch_independence, i5_audit_append_only,
  i6_sizing_fields_in_proposals_are_schema_rejected,
  i6_mind_output_carries_no_executable_side_effects,
  i6_mind_crate_cannot_name_a_venue_or_mutate_state,
  i7_stage_promotion_requires_operator_action_record,
  i7_model_swap_requires_shadow_comparison_record, i7_promotion_gates
- scripts/run-dst.sh 2000                            -> DST_EXIT=0
  "[dst] OK: 0 corpus + 2000 random seeds, zero invariant violations"
  (master seed 1781227600823); synthesis-dst 2000 scenarios ok (master seed
  1781227679737); settlement-dst 2000 scenarios ok (master seed 1781227701308);
  daemon_smoke 2 passed
- Targeted: run_loop 6 passed / daemon_smoke 2 passed / rota 12 passed, 0 failed
- Scratch (worktree-only, then deleted; scratch DB fortuna_scratch_g8 created
  and dropped): real_rota_pool_is_isolated_bounded_and_statement_capped ... ok
  (10.74s); exhausted_pool_renders_the_audit_panel_degraded_200 ... ok (7.37s)
- git diff 7e35f51..468c8c1 --name-only | grep -c fortuna-invariants -> 0
