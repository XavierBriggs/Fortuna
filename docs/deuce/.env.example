# DEUCE — copy to .env.deuce (the project's secret file; also .env works). Both are
# gitignored. Secrets ONLY via these files / env, never committed.

# The Odds API key (https://the-odds-api.com) — your 100K-credit ($59) tier.
# Used only by the CLV layer (Phase B/C); the Phase-A backtest needs no key.
ODDS_API_KEY=REPLACE-ME

# Optional overrides (defaults computed relative to the repo in config.py)
# DEUCE_DATA_DIR=/Users/xavierbriggs/fortuna/data/deuce
