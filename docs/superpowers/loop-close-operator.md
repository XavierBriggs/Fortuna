# loop-close-operator.md — captain's escalation queue + decision log (Loop-Close & Provable Demo milestone)

The captain's action list for the operator + a timestamped log of ambiguities resolved by captain judgment.
Per the charter's **Ambiguity protocol**: the captain resolves ambiguity itself (grounded in invariants → architecture doc → spec → charter), logs it here, and escalates ONLY when irreversible/outward-facing or when the docs genuinely conflict.

## ⚠ Needs operator (irreversible / outward-facing — read these)
_(none open)_

## Decision log (captain judgment — timestamped, newest first)

- **2026-06-19T09:05Z** — **Operating model locked:** synchronous captain loop; roles = principal(captain)/QA(verifier agent)/SDE(builder agent)/architect(planning); flow spec→verify→plan→verify→implement; captain may apply small verifier-flagged nits with verification. Grounds: operator directive + charter. _No escalation needed._
- **2026-06-19T09:05Z** — **§9.1 backtest-vs-promotion:** resolved to conservative default — backtest = evidence + calibration seed; §11 promotion runs on the live-forward clock; backtest rows excluded from the go_nogo count via the `source="historical-import"` stamp. Grounds: CLAUDE.md "spec silent → conservative" + honesty-over-green. _Operator may later opt-in out-of-sample replay to the count (an explicit override) — not blocking._
- **2026-06-19T09:05Z** — **Config-vs-spec divergence:** the demo config + go_nogo use the SPEC §11 values (paper ≥30 trading days, fee/PnL <0.35, ≥60 synthesis beliefs), NOT the shipped example-config values (14 / 0.5 / 100). Divergence recorded in GAPS. Grounds: spec §11 authoritative. _No escalation._
- **2026-06-19T09:05Z** — **D4 + synthesis-binary scope:** D4 (per-producer scoring) is consumed by WS1 (G2+G4). Synthesis BINARY-belief resolution is DEFERRED (synthesis provenance shape is undefined; the demo head-to-head is Aeolus-vs-meteorologist). Grounds: investigation + YAGNI; recorded in GAPS. _No escalation._

- **2026-06-19T09:25Z** — **WS1 plan-verify (iter 1) found 6 Important gaps; resolved 3 design decisions (captain judgment):**
  1. **Synthesis binary resolution DEFERRED** (confirmed: mind.rs:649 synthesis provenance has no `producer`/grading keys). WS1/G2 = unified dispatch + *persona* resolution only; synthesis deferred per spec open-Q#2. (loop-close-gaps.md.) Grounds: investigation + YAGNI.
  2. **Fractional bracket thresholds:** `parse_bracket_hint` (i64-only, aeolus_resolve.rs:79) will be widened to handle fractional thresholds (e.g. `ge87.5`) — Kalshi brackets are fractional (B87.5, per the §7 worked example). Threshold comparison is a temperature/cognition path (not money) → f64/Decimal OK; behavior-preserving for integer brackets. Grounds: real bracket geometry + worked example; else the head-to-head silently skips fractional markets.
  3. **Market-implied baseline Brier** (for the go_nogo Brier-beats-baseline gate): derived by de-vigging the benchmark price snapshot per belief (the SAME benchmark used for CLV), → market-implied p, scored with BrierRule, aggregated per scope. Reuses the Task 4/5 snapshot data — no separate baseline source exists. Grounds: spec §11 "Brier beating market-price baseline".
  _None escalated — all resolvable from spec/code/invariants._

- **2026-06-19T14:49Z** — **WS1 slice 3 scope refinement (captain, YAGNI):** slice 3 = producer-agnostic + FRACTIONAL resolution via a **grammar-agnostic** bracket-hint extraction (rsplit on `[-:#]` → the trailing `ge<n>`/`lt<n>` token; works for both `aeolus:…-ge87` and `weather:…#ge87.5`; removes the daemon.rs:4723 `aeolus:` literal entirely). The unified `PredictiveKind`→`BrierRule` **trait dispatch is DEFERRED to WS2/G5** — with only one rule it equals the existing `brier_score` (no behavior change; verified identical), so building it now is a YAGNI no-op; G5 (RPS/Log) is where a 2nd rule makes the trait dispatch non-vacuous. Head-to-head (the thesis) is fully delivered by slice 3 regardless. Grounds: YAGNI + de-risk the meatiest slice + trait-dispatch is G5's natural prerequisite. _No escalation._

- **2026-06-20T02:31Z** — **WS1 slice 5 (CLV compute) — captain design decisions** (from the Explore map; `fills` have no belief link, so the chain is belief→event→edges→market→fill):
  1. **Entry = the EARLIEST fill** on the belief's event→edge market(s) (`FillsRepo::first_fill_for_market`, ORDER BY at ASC → side+price_cents). No fill on any edge-market → `clv_bps=None` (belief proposed, not traded). Multiple traded markets → earliest fill is the entry, that market's snapshots = the series. Grounds: a bracket belief is ~1 traded market; simplest correct.
  2. **benchmark_at = the event's `benchmark_at` column as-is** (no pre-benchmark offset yet); `clv_bps` picks the last liquid snapshot before it. A pre-benchmark window offset is a future refinement (GAPS).
  3. **Persist `clv_bps` as the BPS value in f64** (e.g. 83.0 for +83bps) — do NOT divide by 10000 (the Explore agent's draft did  → would store 0.0083, misrepresenting the bps column). The column is basis points.
  4. **Market-implied p for slice 8b = computed ON-DEMAND in 8b** from the benchmark snapshot (de-vig: YES→mid, NO→1−mid) — slice 5 does NOT thread/persist it (deterministic from bid/ask; YAGNI). Drops that sub-task from slice 5.
  5. **LiquidityPolicy** (min_touch_qty, max_spread_cents): use an existing config value if one exists; else a documented default + GAPS note.
  _No escalation — all resolvable from the code map + spec 5.5._

- **2026-06-20T05:43Z** — **WS1 slice 7 (synthesis prices best producer) — captain scope decision:** calibration PARAMS are NOT producer-keyed yet (runner.rs:757: "producer deferred to D4 — passed as None"). So slice 7 = per-producer **QUALITY** selection: synthesis's calibration quality (the h_cal sizing haircut input) is set to the **best-calibrated producer's quality** (via resolved_stats_for_producer). calibration_for_scope gains `producer: Option<&str>` that scopes ONLY the quality computation (params stay model_id-keyed). Candidate producers come from a DATA query (DISTINCT resolved producers in the category); best = quality DESC, producer-id ASC; NO producer literal. **Per-producer calibration PARAMS persistence (keying the Platt fit by producer) is DEFERRED** — it needs a persist/schema change, and the quality-based selection already delivers the actionable head-to-head (synthesis sizes by the better producer's calibration). Grounds: YAGNI + thesis delivered by quality-selection. (loop-close-gaps.)

- **2026-06-20T19:40Z** — **First GENUINE meteorologist finding produced on LIVE data — and two charter bugs found+fixed that were silently breaking the findings→beliefs loop.** Operator asked to run one live meteorologist analysis ("see the reasoning we got"); the honest pre-answer was *we had none*, and this run found out WHY: the persona path had never worked end-to-end. Built a gated live one-shot, `crates/fortuna-cognition/tests/persona_live_smoke.rs`, that drives the REAL `run_due_personas` with a REAL `ReqwestMindTransport` over today's LIVE Aeolus KNYC envelope + live NWS AFD — the exact daemon cognition path, one manual tick, no Postgres. Two real defects surfaced (neither caught by the scripted-`StubMind` suite, which hand-builds the output shape):
  1. **[FIXED] Charter never routed the finding into `journal.body`.** The persona path forces the synthesis `{beliefs,proposals,journal}` output schema (`call_priced` → `output_schema()`), and `run_persona_analysis` reads the finding from `journal.body`; the shipped persona.md never said so. Real Opus therefore filled `beliefs` with a human-phrased `horizon` ("~1 day (target …)") → strict `MindOutput` parse rejected the entire output → **zero findings, ever** (consistent with the empty DBs). Fix: added a "How your finding is transported" section — `beliefs:[]`, `proposals:[]` (also the correct I6 behavior: the persona authors no beliefs), finding as a JSON string in `journal.body`.
  2. **[FIXED] Threshold field-name drift.** Real Opus emitted `threshold_f` (mirroring the Aeolus bracket field) instead of the schema's `ge`. `validate_findings` PASSED it (it checks only top-level `required`/`additionalProperties`, never recurses), but `map_persona_analysis` (persona_beliefs.rs:156) requires `ge` → beliefs would not map. Fix: persona.md now names the threshold object fields explicitly (`{ge,p}`, "use `ge` — NOT `threshold_f`").
  After both fixes the harness produced a genuine, **loop-valid** finding — NYC tmax 2026-06-21, μ=79.57/σ=2.34, cost 12¢, content_hash `0917…`: anchored on the envelope, named the real synoptic regime from the live AFD ("broad upper trough, post-frontal cyclonic NW flow under a moderating Canadian airmass"), flagged the negative `crpss_vs_raw` (−0.16) as a caution against tightening, two-sided `key_risk`, ge72→ge87 monotone. Charter kept at **v3** (these are output-PLUMBING completions, not a reasoning-method change). 16 persona unit tests + the WS1 region_key mutation-proof remain green. Grounds: honesty-over-green + close-the-loop. _Follow-ups ledgered (registry version bump, validator hardening, persona-tier timeout); none escalated._

- **2026-06-20T21:30Z** — **Ran a (proportionate) Hephaestus cascade to close the two top follow-ups; PAUSED at WS2 per operator.** Operator: "use the hephaestus loop … don't blow this up … pause at ws2." Right-sized it: the three roles + gate discipline (hp-implementer → independent hp-verifier per slice; hp-guardian at the final gate; machine-green before done), but NO separate worktree (foundation lives on this branch), NO infinite ralph wrapper (drove the cascade directly), scope frozen to two slices, no commits. **Slice A** (validate_findings recursion) and **Slice B** (meteorologist v3→v4 surgical promotion) each PASSED an independent verify (mutation-proofs: 4 killed on A; the version-mismatch safety gate's bite re-proven on B); **hp-guardian PASS** (no scope drift, fortuna-invariants untouched, I6 honored, no faked green, both Importants from the gaps ledger genuinely closed). **Final live gate surfaced a NEW out-of-scope finding** (handled honestly, NOT masked): the persona's `journal.body` routing is intermittently unreliable (one run returned `journal:null` → no finding; fail-closed, 10¢) — pre-existing to A/B, root-caused to the nullable-journal synthesis schema; the robust fix (persona structured-output via `call_priced_structured`) is ledgered as the **top next-priority, on the demo's critical path** (spec §8/§7) and is the natural first WS2 slice. Guardian confirmed pausing here is correct (the failure is non-corrupting; honoring the operator instruction with the fix captured is disciplined, not unsafe deferral). Grounds: operator directive + guardian PASS. _No escalation; awaiting operator "go WS2"._
