# Review: phase-2-gate — 2026-06-10
Base: c1e206f  Head: 1dcd05d ("Phase 2 EXIT")  Verdict: BLOCK
Protected crate touched: yes (crates/fortuna-invariants/ — Cargo.toml, tests/i6_propose_only_mind.rs, tests/i7_promotion_gates.rs; automatic BLOCK pending operator review)

All executable checks ran at commit 1dcd05d in a detached scratch worktree
(/tmp/fortuna-gate-p2), NOT against the operator's dirty working tree (in-progress
Phase 3 work by a parallel session). DATABASE_URL=postgres://localhost/fortuna_dev.

## Criteria (fixed from BUILD_PLAN.md lines 193-195 before reading the diff)

- C1 Full decision loop in Sim with StubMind under DST, including cognition failure
  scenarios (spec 5.8, 5.9, 5.14): **FAIL** — the composed loop runs green under DST
  (3000 seeds, zero violations, byte-identical replay, 39,989 injected cognition
  failures degraded without a tick error; synthesis_loop.rs proves
  belief→comparator→proposal→sizing→gates→fill with StubMind), BUT it is not the FULL
  decision loop the spec defines. Spec 5.8: "Calibration layer adjusts p. Decision
  engine derives orders (sizing below)"; spec 5.9/line 240: "fractional Kelly (default
  0.25) on calibrated edge ... haircut by category calibration quality." Neither stage
  is in the composition: p is used raw (cycle.rs:302-304 comment defers calibration to
  "the live composition"); the runner sizes every proposal with affordable_sets
  (runner.rs:449 — full envelope headroom up to caps); `kelly_binary`,
  `kelly_contracts`, and `haircut_kelly_fraction` have ZERO src call sites (grep;
  only the fortuna-state re-export and unit tests). cycle.rs's own header claims "the
  runner draws from the envelope via reservations using the haircut Kelly fraction" —
  demonstrably false. See F2.
- C2 AnthropicMind exercised behind a feature flag with budget controls (spec 5.9;
  PROMPT.md acceptance: "Mind trait with a stub mind AND the Anthropic-backed mind ...
  runs end-to-end in Sim"): **UNVERIFIABLE → FAIL** — live exercise is
  operator-blocked: ANTHROPIC_API_KEY is unset (verified in-session) and GAPS.md
  documents it: "the Phase 2 exit's 'AnthropicMind exercised behind a feature flag'
  needs ANTHROPIC_API_KEY — the env-key gate IS the flag; one live smoke call against
  claude-haiku-4-5 with a tight CostBudget is the recommended first exercise."
  Mock-transport exercise of decide_with_budget is green (6 tests in
  fortuna-cognition/tests/mind.rs). However two structural gaps go beyond the operator
  block: AnthropicMind does not implement the Mind trait (spec 5.9: "both behind
  `Mind`") and nothing outside tests constructs AnthropicMind or ReqwestMindTransport —
  even with the key provided, no composition path can exercise it (F3); and the
  per-cycle budget control is vacuous for any positive cap (F4).
- C3 aeolus_eval writes scored beliefs from fixture envelopes (spec Section 6 item 3):
  **PASS** — fixtures/aeolus/sample_envelope.json → strict AeolusEnvelope parse
  (deny_unknown_fields) → zero-capital BeliefDrafts (map_aeolus_envelope returns
  BeliefDrafts only; no proposal type in the path) → BeliefsRepo insert →
  resolve_and_score (score-once enforced) → calibration_quality sees the samples.
  Executed: `test aeolus_eval_writes_scored_beliefs_from_the_fixture_envelope ... ok`
  (fortuna-ledger/tests/ledger.rs, workspace run line 483). The fixture is the
  FORTUNA-defined contract sample; the operator-recorded real Aeolus export remains an
  open GAPS item with exact unblock instructions (validates Aeolus's exporter, not
  FORTUNA's parser) — within the EXIT line's wording.
- C4 I5–I7 invariant tests green: **PASS (with operator adjudication required)** —
  executed `cargo test -p fortuna-invariants`: i5_audit_append_only ok;
  i6_sizing_fields_in_proposals_are_schema_rejected ok,
  i6_mind_output_carries_no_executable_side_effects ok,
  i6_mind_crate_cannot_name_a_venue_or_mutate_state ok; i7_promotion_gates ok;
  0 failed. i7 carries TWO new #[ignore]+todo!() staged stubs
  (i7_stage_promotion_requires_operator_action_record → T3.1,
  i7_model_swap_requires_shadow_comparison_record → T3.3). The base-commit i7 stub's
  ownership annotation read "T2.6/T3.3"; the operator-action-record clause was
  re-staged to T3.1. GAPS.md documents both under "Open". Inside the protected crate —
  operator adjudicates (F1/F5).
- C5 Standing hygiene (CLAUDE.md definition of done + fortuna-review mechanical
  sweep): **PASS with Minors** — cargo fmt --check exit 0; clippy --workspace
  --all-targets -D warnings exit 0; workspace tests 580 passed / 0 failed / 2 ignored
  (the two staged i7 stubs); sweep of added src lines: no unwrap/expect/panic!/todo!,
  no SystemTime::now/Instant::now/Utc::now outside Clock impls, no f32, no HashMap
  iteration feeding bus/audit/sizing (CoreHandle.books is BTreeMap), no new `place(`
  call sites, no GatedOrder constructors outside fortuna-gates, no proptest case
  reductions, no deleted assertions, no secrets (the 26 new JSON files are sqlx
  offline metadata + the aeolus fixture; API key is env-only, Debug-redacted). Minors:
  F6, F7, F8.

GATE RESULT: Phase 2 EXIT is NOT met. C1 FAIL, C2 UNVERIFIABLE/FAIL; plus automatic
BLOCK on the protected-crate touch regardless of content.

## Findings

- [Critical] F1 Protected crate touched: crates/fortuna-invariants/ modified
  (Cargo.toml +4 deps; i6 and i7 stub files replaced with implementations) —
  automatic BLOCK pending operator review per CLAUDE.md. Content analysis for the
  operator: the changes implement former #[ignore]+todo!() stubs in their owning
  tasks' slots, exactly the pattern the pre-existing
  crates/fortuna-invariants/tests/README.md sanctions ("Implementing the test
  (removing #[ignore], writing the real assertions ...) is part of the owning
  BUILD_PLAN task"); no pre-existing assertion logic existed to weaken (both files
  were todo!() stubs at c1e206f); 3 i6 tests + 1 i7 test ADDED and green; 2 new
  staged stubs ADDED. Reproduction: `git diff c1e206f...1dcd05d --
  crates/fortuna-invariants/`.
- [Major] F2 The composed decision loop omits the spec's sizing stages for
  model-originated trades: no calibration-adjusted p, no fractional Kelly, no
  calibration-quality haircut. Spec 5.8: "Calibration layer adjusts p."; spec line
  240: "fractional Kelly (default 0.25) on calibrated edge, drawing from the
  strategy's envelope via reservation, haircut by category calibration quality."
  Actual: runner.rs:449 `affordable_sets(headroom, cost_per_set,
  max_sets_per_proposal)` — spends ALL strategy headroom up to caps regardless of
  edge size or calibration quality (anti-conservative vs spec). Evidence: `grep -rn
  "kelly_contracts|kelly_binary|haircut_kelly_fraction" crates/*/src` → zero call
  sites outside definitions/re-export. cycle.rs:9-10 doc claim ("the runner draws
  from the envelope via reservations using the haircut Kelly fraction") is false;
  BUILD_PLAN T2.6/T2.8 DONE notes claim this wiring ("quality ... -> T2.6 haircut").
  Fails C1's "full decision loop".
- [Major] F3 AnthropicMind is not behind the Mind trait — spec 5.9: "Tiering: triage
  model (cheap), synthesis model (Fable 5), both behind `Mind`." Only StubMind (and
  two test minds) implement Mind; AnthropicMind exposes the inherent
  decide_with_budget only, and no binary or composition constructs AnthropicMind or
  ReqwestMindTransport (grep: from_env call sites = ops Slack config + mind.rs
  itself). Even with ANTHROPIC_API_KEY provided, nothing would exercise it in the
  loop without new code — C2 is not merely operator-blocked, it is not
  buildable-complete.
- [Major] F4 Per-cycle cost budget is non-functional for any positive cap:
  CostBudget::check (mind.rs:221-238) refuses only when per_cycle_cap_cents <= 0 and
  otherwise compares only per-DAY spend; no estimate or post-hoc comparison ever
  consults the per-cycle cap, so a single call exceeding it is neither prevented nor
  flagged. Spec 5.9: "Per-cycle and per-day cost budgets in config; budget breach
  degrades to mechanical-only and alerts." The only test coverage is the degenerate
  cap=0 case (tests/mind.rs budgets_check_before_calling_and_roll_at_utc_midnight).
- [Major] F5 Two I7 invariant clauses remain unimplemented #[ignore]+todo!() stubs
  inside the protected crate, with the operator-action-record clause re-staged from
  the base stub's "T2.6/T3.3" ownership annotation to T3.1 (whose BUILD_PLAN text is
  "lesson promotion flow", not stage promotion). GAPS.md documents it; the Phase 3
  acceptance checklist (zero ignored tests) backstops it. Operator adjudicates
  alongside F1 whether "I5-I7 invariant tests green" tolerates staged clauses.
- [Minor] F6 DecisionCycle::run silently discards the model's ProposalDrafts
  (output.proposals is never read, counted, or audited — contrast
  reconciliation.rs's discarded_proposals counter), and the composed Sim loop writes
  no model_call audit record (spec Section 7 typed audit records include model_call);
  CycleOutcome.manifest_hash is dropped by SynthesisStrategy. Conservative direction
  (fewer trade paths) but unaudited model output.
- [Minor] F7 f64 arithmetic at the probability→cents conversion boundary:
  cycle.rs:117 `(market_p * 100.0).floor() as i64` instead of rust_decimal per house
  style. Direction is conservative (floor never rounds an edge into existence; p
  strictly inside (0,1)).
- [Minor] F8 Junk binaries committed in the range: docs/.DS_Store modified,
  docs/research/.DS_Store and docs/research/venue/.DS_Store added.

## Commands run (verbatim verdict lines; all at 1dcd05d in /tmp/fortuna-gate-p2)

```
$ git worktree add --detach /tmp/fortuna-gate-p2 1dcd05d
HEAD is now at 1dcd05d Phase 2 EXIT: composed decision loop in Sim under DST, I6/I7 invariants, aeolus_eval end to end

$ cargo fmt --check
(exit 0, no output)

$ DATABASE_URL=postgres://localhost/fortuna_dev cargo clippy --workspace --all-targets -- -D warnings
Finished `dev` profile [unoptimized + debuginfo] target(s) in 18.22s   (exit 0)

$ DATABASE_URL=postgres://localhost/fortuna_dev cargo test --workspace
EXIT=0
TOTAL passed=580 failed=0 ignored=2

$ DATABASE_URL=postgres://localhost/fortuna_dev cargo test -p fortuna-invariants
i1: 2 passed; i2: 2 passed; i3: 1 passed; i4: 1 passed; i5: 1 passed;
i6: 3 passed; i7: 1 passed, 2 ignored (T3.1/T3.3 staged stubs); doc-tests: 3 passed; 0 failed

$ DST_MASTER_SEED=20260610 scripts/run-dst.sh 3000
[dst] regression corpus: 0 seed(s)
[dst] master seed 20260610 -> 3000 random scenario(s)
[dst] OK: 0 corpus + 3000 random seeds, zero invariant violations
[synthesis-dst] master seed 20260610 -> 3000 scenario(s)
[synthesis-dst] totals: 10139 orders, 15627 proposals, 39989 cognition failures, 35838 beliefs
test result: ok. 1 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out; finished in 30.16s
DST_EXIT=0

$ [ -n "$ANTHROPIC_API_KEY" ] && echo set || echo unset
unset

$ grep -rn "kelly_contracts|kelly_binary" crates/*/src (excluding sizing.rs, tests)
crates/fortuna-state/src/lib.rs:42: pub use sizing::{affordable_sets, kelly_binary, kelly_contracts};   (re-export only; zero call sites)

$ grep "haircut_kelly_fraction" crates (src, excluding cycle.rs definition)
(no matches)

Mechanical sweeps over `git diff c1e206f...1dcd05d`:
new #[ignore]: only the two T3.1/T3.3 staged stubs in fortuna-invariants
deleted assert / proptest reductions / .place( additions / secret literals: none
```

Operator paths forward: (1) adjudicate the fortuna-invariants diff (F1/F5 — content
appears to be the sanctioned stub-implementation pattern); (2) wire calibration apply
+ Kelly haircut into the composed sizing path or amend the spec/EXIT claim (F2);
(3) put AnthropicMind behind Mind and give it a composition path, then perform the
GAPS-recommended haiku smoke call under a tight CostBudget (F3, C2 unblock);
(4) make the per-cycle budget bind (F4).
