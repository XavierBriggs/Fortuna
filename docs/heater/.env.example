# HEATER — copy to .env.heater (the project's secret file; also .env works). Both are
# gitignored. Secrets ONLY via these files / env, never committed.

# The Odds API key (https://the-odds-api.com) — used only by the CLV layer (Phase B/C).
# The Phase-A backtest (synthetic OR Statcast) needs no key.
ODDS_API_KEY=REPLACE-ME

# Optional override (defaults to <repo>/data/heater in config.py)
# HEATER_DATA_DIR=/Users/xavierbriggs/fortuna/data/heater
